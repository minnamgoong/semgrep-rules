# Global Semgrep Rules Summary

# Language: AI

## CSHARP

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-openai.yaml` |
## DART

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-gemini.yaml` |
## GENERIC

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-generic-ai-anthprop` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-generic-ai-anthprop.yaml` |
| `detect-generic-ai-api` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-generic-ai-api.yaml` |
| `detect-generic-ai-gem` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-generic-ai-gem.yaml` |
| `detect-generic-ai-oai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-generic-ai-oai.yaml` |
## GO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-gemini.yaml` |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-openai.yaml` |
## KOTLIN

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-gemini.yaml` |
## PYTHON

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-anthropic` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-anthropic.yaml` |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-gemini.yaml` |
| `detect-huggingface` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-huggingface.yaml` |
| `detect-langchain` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-langchain.yaml` |
| `detect-mistral` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-mistral.yaml` |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-openai.yaml` |
| `detect-pytorch` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-pytorch.yaml` |
| `detect-tensorflow` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-tensorflow.yaml` |
## SWIFT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-apple-core-ml` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-apple-core-ml.yaml` |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-gemini.yaml` |
## TYPESCRIPT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-anthropic` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-anthropic.yaml` |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-gemini.yaml` |
| `detect-mistral` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-mistral.yaml` |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-openai.yaml` |
| `detect-promptfoo` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-promptfoo.yaml` |
| `detect-vercel-ai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `ai/detect-vercel-ai.yaml` |
# Language: BASH

## CURL

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `curl-eval` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `bash/security/curl-eval.yaml` |
| `curl-pipe-bash` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `bash/security/curl-pipe-bash.yaml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `ifs-tampering` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `bash/security/ifs-tampering.yaml` |
| `iteration-over-ls-output` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `bash/best-practice/iteration-over-ls-output.yaml` |
| `unquoted-variable-expansion-in-command` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `bash/correctness/unquoted-expansion.yaml` |
| `useless-cat` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `bash/best-practice/useless-cat.yaml` |
# Language: DOCKERFILE

## AUDIT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: dockerfile-pip-extra-index-url` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/dockerfile-pip-extra-index-url.yaml` |
| `- id: dockerfile-source-not-pinned` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/dockerfile-source-not-pinned.yaml` |
## BEST-PRACTICE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `avoid-apk-upgrade` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/avoid-apk-upgrade.yaml` |
| `avoid-apt-get-upgrade` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/avoid-apt-get-upgrade.yaml` |
| `avoid-dnf-update` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/avoid-dnf-update.yaml` |
| `avoid-latest-version` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/avoid-latest-version.yaml` |
| `avoid-platform-with-from` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/avoid-platform-with-from.yaml` |
| `avoid-yum-update` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/avoid-yum-update.yaml` |
| `avoid-zypper-update` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/avoid-zypper-update.yaml` |
| `maintainer-is-deprecated` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/maintainer-is-deprecated.yaml` |
| `missing-apk-no-cache` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/missing-apk-no-cache.yaml` |
| `missing-dnf-assume-yes-switch` | `WARNING` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/missing-dnf-assume-yes-switch.yaml` |
| `missing-dnf-clean-all` | `WARNING` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/missing-dnf-clean-all.yaml` |
| `missing-image-version` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/missing-image-version.yaml` |
| `missing-no-install-recommends` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/missing-no-install-recommends.yaml` |
| `missing-pip-no-cache-dir` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/missing-pip-no-cache-dir.yaml` |
| `missing-yum-assume-yes-switch` | `WARNING` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/missing-yum-assume-yes-switch.yaml` |
| `missing-zypper-clean` | `WARNING` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/missing-zypper-clean.yaml` |
| `nonsensical-command` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/nonsensical-command.yaml` |
| `prefer-apt-get` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/prefer-apt-get.yaml` |
| `prefer-copy-over-add` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/prefer-copy-over-add.yaml` |
| `prefer-json-notation` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/prefer-json-notation.yaml` |
| `remove-package-cache` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/remove-package-cache.yaml` |
| `remove-package-lists` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/remove-package-lists.yaml` |
| `set-pipefail` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/set-pipefail.yaml` |
| `use-either-wget-or-curl` | `INFO` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/use-either-wget-or-curl.yaml` |
| `use-shell-instruction` | `WARNING` | Dockerfile 가동 중 베스트 프랙티스(Best Practice) 점검. | `dockerfile/use-shell-instruction.yaml` |
| `use-workdir` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/use-workdir.yaml` |
## CORRECTNESS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `invalid-port` | `ERROR` | Dockerfile 연쇄 점검: invalid-port 준행 오류 국면 감지. | `dockerfile/invalid-port.yaml` |
| `- id: missing-assume-yes-switch` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/missing-assume-yes-switch.yaml` |
| `multiple-entrypoint-instructions` | `ERROR` | ENTRYPOINT 명령어 다수 사용 시 충돌 국면 점검. | `dockerfile/multiple-entrypoint-instructions.yaml` |
## SECURITY

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `dockerfile-dockerd-socket-mount` | `ERROR` | Dockerfile 연쇄 점검: dockerfile-dockerd-socket-mount 준행 오류 국면 감지. | `dockerfile/dockerd-socket-mount.yaml` |
| `last-user-is-root` | `ERROR` | 마지막 실행 컨텍스트가 root로 전사 탑재 되었습니다. | `dockerfile/last-user-is-root.yaml` |
| `missing-user` | `ERROR` | Dockerfile에 USER 명령어가 부재하여 root 권한 폭주 우려가 있습니다. | `dockerfile/missing-user.yaml` |
| `missing-user-entrypoint` | `ERROR` | USER 명령어 누사로 인한 컨테이너 root 실행 위험 지점입니다. | `dockerfile/missing-user-entrypoint.yaml` |
| `- id: no-sudo-in-dockerfile` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `dockerfile/no-sudo-in-dockerfile.yaml` |
| `- id: secret-in-build-arg` | `WARNING` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `dockerfile/secret-in-build-arg.yaml` |
# Language: GENERIC

## BICEP

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: secure-parameter-for-secrets` | `WARNING` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/security/secure-parameter-for-secrets.yaml` |
## CI

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `bash_reverse_shell` | `ERROR` | bash_reverse_shell 보안 인젝션 및 런타임 버그 전사 예방 점검. | `generic/security/bash-reverse-shell.yaml` |
| `- id: changed-semgrepignore` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/audit/changed-semgrepignore.yaml` |
## DOCKERFILE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `alias-must-be-unique` | `ERROR` | Alias 식별자 고유율을 수하 하세요. | `generic/correctness/alias-must-be-unique.yaml` |
| `copy-from-own-alias` | `ERROR` | 자기 자신의 스테이지 레이어를 출처(from)로 명명하는 순환 구성을 경보합니다. | `generic/correctness/copy-from-own-alias.yaml` |
| `missing-yum-clean-all` | `WARNING` | missing-yum-clean-all 잠재적 결함 분기 정비 요망. | `generic/best-practice/missing-yum-clean-all.yaml` |
| `missing-zypper-no-confirm-switch` | `WARNING` | missing-zypper-no-confirm-switch 잠재적 결함 분기 정비 요망. | `generic/missing-zypper-no-confirm-switch.yaml` |
| `multiple-cmd-instructions` | `ERROR` | Dockerfile 내 `CMD` 절 중복 가압 금지. 마지막 절만 효력을 발휘합니다. | `generic/correctness/multiple-cmd-instructions.yaml` |
| `- id: use-absolute-workdir` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/best-practice/use-absolute-workdir.yaml` |
## GRADLE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: build-gradle-password-hardcoded` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/security/build-gradle-password-hardcoded.yaml` |
## HTML-TEMPLATES

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: unquoted-attribute-var` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/security/unquoted-attribute-var.yaml` |
| `- id: var-in-href` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/security/var-in-href.yaml` |
| `- id: var-in-script-src` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/security/var-in-script-src.yaml` |
| `- id: var-in-script-tag` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/security/var-in-script-tag.yaml` |
## HUGO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `invalid-base-url` | `WARNING` | invalid-base-url 잠재적 결함 분기 정비 요망. | `generic/best-practice/invalid-base-url.yaml` |
| `localhost-base-url` | `WARNING` | localhost-base-url 잠재적 결함 분기 정비 요망. | `generic/best-practice/localhost-base-url.yaml` |
## NGINX

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `alias-path-traversal` | `WARNING` | alias-path-traversal 잠재적 결함 분기 정비 요망. | `generic/security/alias-path-traversal.yaml` |
| `dynamic-proxy-host` | `WARNING` | dynamic-proxy-host 잠재적 결함 분기 정비 요망. | `generic/security/dynamic-proxy-host.yaml` |
| `dynamic-proxy-scheme` | `WARNING` | dynamic-proxy-scheme 잠재적 결함 분기 정비 요망. | `generic/security/dynamic-proxy-scheme.yaml` |
| `header-injection` | `ERROR` | Nginx 헤더 가압 버퍼 오염 위험 경고. | `generic/security/header-injection.yaml` |
| `header-redefinition` | `WARNING` | header-redefinition 잠재적 결함 분기 정비 요망. | `generic/security/header-redefinition.yaml` |
| `- id: insecure-redirect` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `generic/security/insecure-redirect.yaml` |
| `insecure-ssl-version` | `WARNING` | 보안 옵션 누사 및 예방 단속 피로도 증대. | `generic/security/insecure-ssl-version.yaml` |
| `missing-internal` | `WARNING` | missing-internal 잠재적 결함 분기 정비 요망. | `generic/security/missing-internal.yaml` |
| `missing-ssl-version` | `WARNING` | missing-ssl-version 잠재적 결함 분기 정비 요망. | `generic/security/missing-ssl-version.yaml` |
| `possible-nginx-h2c-smuggling` | `WARNING` | possible-nginx-h2c-smuggling 잠재적 결함 분기 정비 요망. | `generic/security/possible-h2c-smuggling.yaml` |
| `request-host-used` | `WARNING` | request-host-used 잠재적 결함 분기 정비 요망. | `generic/security/request-host-used.yaml` |
## SECRETS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: adafruit-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/adafruit-api-key.yaml` |
| `- id: adobe-client-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/adobe-client-id.yaml` |
| `- id: adobe-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/adobe-client-secret.yaml` |
| `- id: age-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/age-secret-key.yaml` |
| `- id: airtable-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/airtable-api-key.yaml` |
| `- id: algolia-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/algolia-api-key.yaml` |
| `- id: alibaba-access-key-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/alibaba-access-key-id.yaml` |
| `- id: alibaba-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/alibaba-secret-key.yaml` |
| `- id: asana-client-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/asana-client-id.yaml` |
| `- id: asana-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/asana-client-secret.yaml` |
| `- id: atlassian-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/atlassian-api-token.yaml` |
| `- id: authress-service-client-access-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/authress-service-client-access-key.yaml` |
| `- id: aws-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/aws-access-token.yaml` |
| `- id: beamer-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/beamer-api-token.yaml` |
| `- id: bitbucket-client-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/bitbucket-client-id.yaml` |
| `- id: bitbucket-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/bitbucket-client-secret.yaml` |
| `- id: bittrex-access-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/bittrex-access-key.yaml` |
| `- id: bittrex-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/bittrex-secret-key.yaml` |
| `- id: clojars-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/clojars-api-token.yaml` |
| `- id: cloudflare-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/cloudflare-api-key.yaml` |
| `- id: cloudflare-global-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/cloudflare-global-api-key.yaml` |
| `- id: cloudflare-origin-ca-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/cloudflare-origin-ca-key.yaml` |
| `- id: codecov-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/codecov-access-token.yaml` |
| `- id: coinbase-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/coinbase-access-token.yaml` |
| `- id: confluent-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/confluent-access-token.yaml` |
| `- id: confluent-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/confluent-secret-key.yaml` |
| `- id: contentful-delivery-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/contentful-delivery-api-token.yaml` |
| `- id: databricks-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/databricks-api-token.yaml` |
| `- id: datadog-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/datadog-access-token.yaml` |
| `- id: defined-networking-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/defined-networking-api-token.yaml` |
| `detected-amazon-mws-auth-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-amazon-mws-auth-token.yaml` |
| `detected-artifactory-password` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-artifactory-password.yaml` |
| `detected-artifactory-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-artifactory-token.yaml` |
| `detected-aws-access-key-id-value` | `ERROR` | AWS Access/Secret 자격증명이 노출되었습니다. | `generic/security/detected-aws-access-key-id-value.yaml` |
| `detected-aws-account-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/security/detected-aws-account-id.yaml` |
| `detected-aws-appsync-graphql-key` | `ERROR` | AWS Access/Secret 자격증명이 노출되었습니다. | `generic/security/detected-aws-appsync-graphql-key.yaml` |
| `detected-aws-secret-access-key` | `ERROR` | AWS Access/Secret 자격증명이 노출되었습니다. | `generic/security/detected-aws-secret-access-key.yaml` |
| `detected-aws-session-token` | `ERROR` | AWS Access/Secret 자격증명이 노출되었습니다. | `generic/security/detected-aws-session-token.yaml` |
| `detected-bcrypt-hash` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-bcrypt-hash.yaml` |
| `detected-codeclimate` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-codeclimate.yaml` |
| `detected-etc-shadow` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-etc-shadow.yaml` |
| `detected-facebook-access-token` | `ERROR` | Facebook 인증 토큰이 노출되었습니다. | `generic/security/detected-facebook-access-token.yaml` |
| `detected-facebook-oauth` | `ERROR` | Facebook 인증 토큰이 노출되었습니다. | `generic/security/detected-facebook-oauth.yaml` |
| `detected-generic-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-generic-api-key.yaml` |
| `detected-generic-secret` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-generic-secret.yaml` |
| `detected-github-token` | `ERROR` | GitHub 토큰 자격증명이 노출되었습니다. | `generic/security/detected-github-token.yaml` |
| `detected-google-api-key` | `ERROR` | Google API/OAuth 자격증명이 노출되었습니다. | `generic/security/detected-google-api-key.yaml` |
| `detected-google-cloud-api-key` | `ERROR` | Google API/OAuth 자격증명이 노출되었습니다. | `generic/security/detected-google-cloud-api-key.yaml` |
| `detected-google-gcm-service-account` | `ERROR` | Google API/OAuth 자격증명이 노출되었습니다. | `generic/security/detected-google-gcm-service-account.yaml` |
| `detected-google-oauth-access-token` | `ERROR` | Google API/OAuth 자격증명이 노출되었습니다. | `generic/security/detected-google-oauth-access-token.yaml` |
| `detected-google-oauth-url` | `ERROR` | Google API/OAuth 자격증명이 노출되었습니다. | `generic/security/detected-google-oauth.yaml` |
| `detected-heroku-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-heroku-api-key.yaml` |
| `detected-hockeyapp` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-hockeyapp.yaml` |
| `detected-jwt-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-jwt-token.yaml` |
| `detected-kolide-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-kolide-api-key.yaml` |
| `detected-mailchimp-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-mailchimp-api-key.yaml` |
| `detected-mailgun-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-mailgun-api-key.yaml` |
| `detected-npm-registry-auth-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-npm-registry-auth-token.yaml` |
| `detected-onfido-live-api-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-onfido-live-api-token.yaml` |
| `detected-outlook-team` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-outlook-team.yaml` |
| `detected-paypal-braintree-access-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-paypal-braintree-access-token.yaml` |
| `detected-pgp-private-key-block` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-pgp-private-key-block.yaml` |
| `detected-picatic-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-picatic-api-key.yaml` |
| `detected-private-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-private-key.yaml` |
| `detected-sauce-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-sauce-token.yaml` |
| `detected-sendgrid-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-sendgrid-api-key.yaml` |
| `detected-slack-token` | `ERROR` | Slack 토큰 자격증명이 노출되었습니다. | `generic/security/detected-slack-token.yaml` |
| `detected-slack-webhook` | `ERROR` | Slack 토큰 자격증명이 노출되었습니다. | `generic/security/detected-slack-webhook.yaml` |
| `detected-snyk-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-snyk-api-key.yaml` |
| `detected-softlayer-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-softlayer-api-key.yaml` |
| `detected-sonarqube-docs-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-sonarqube-docs-api-key.yaml` |
| `detected-square-access-token` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-square-access-token.yaml` |
| `detected-square-oauth-secret` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-square-oauth-secret.yaml` |
| `detected-ssh-password` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-ssh-password.yaml` |
| `detected-stripe-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-stripe-api-key.yaml` |
| `detected-stripe-restricted-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-stripe-restricted-api-key.yaml` |
| `detected-telegram-bot-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-telegram-bot-api-key.yaml` |
| `detected-twilio-api-key` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-twilio-api-key.yaml` |
| `detected-username-and-password-in-uri` | `ERROR` | 보안 기밀/인증 문자열(Secret) 노출이 감지되었습니다. 보안 저장소로 대두 전담 하세요. | `generic/security/detected-username-and-password-in-uri.yaml` |
| `- id: digitalocean-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/digitalocean-access-token.yaml` |
| `- id: digitalocean-pat` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/digitalocean-pat.yaml` |
| `- id: digitalocean-refresh-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/digitalocean-refresh-token.yaml` |
| `- id: discord-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/discord-api-token.yaml` |
| `- id: discord-client-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/discord-client-id.yaml` |
| `- id: discord-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/discord-client-secret.yaml` |
| `- id: doppler-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/doppler-api-token.yaml` |
| `- id: droneci-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/droneci-access-token.yaml` |
| `- id: dropbox-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/dropbox-api-token.yaml` |
| `- id: dropbox-long-lived-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/dropbox-long-lived-api-token.yaml` |
| `- id: dropbox-short-lived-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/dropbox-short-lived-api-token.yaml` |
| `- id: duffel-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/duffel-api-token.yaml` |
| `- id: dynatrace-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/dynatrace-api-token.yaml` |
| `- id: easypost-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/easypost-api-token.yaml` |
| `- id: easypost-test-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/easypost-test-api-token.yaml` |
| `- id: etsy-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/etsy-access-token.yaml` |
| `- id: facebook` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/facebook.yaml` |
| `- id: facebook-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/facebook-access-token.yaml` |
| `- id: facebook-page-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/facebook-page-access-token.yaml` |
| `- id: facebook-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/facebook-secret.yaml` |
| `- id: fastly-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/fastly-api-token.yaml` |
| `- id: finicity-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/finicity-api-token.yaml` |
| `- id: finicity-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/finicity-client-secret.yaml` |
| `- id: finnhub-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/finnhub-access-token.yaml` |
| `- id: flickr-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/flickr-access-token.yaml` |
| `- id: flutterwave-encryption-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/flutterwave-encryption-key.yaml` |
| `- id: flutterwave-public-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/flutterwave-public-key.yaml` |
| `- id: flutterwave-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/flutterwave-secret-key.yaml` |
| `- id: frameio-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/frameio-api-token.yaml` |
| `- id: freshbooks-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/freshbooks-access-token.yaml` |
| `- id: gcp-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/gcp-api-key.yaml` |
| `- id: generic-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/generic-api-key.yaml` |
| `- id: github-app-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/github-app-token.yaml` |
| `- id: github-fine-grained-pat` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/github-fine-grained-pat.yaml` |
| `- id: github-oauth` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/github-oauth.yaml` |
| `- id: github-pat` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/github-pat.yaml` |
| `- id: github-refresh-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/github-refresh-token.yaml` |
| `- id: gitlab-pat` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/gitlab-pat.yaml` |
| `- id: gitlab-ptt` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/gitlab-ptt.yaml` |
| `- id: gitlab-rrt` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/gitlab-rrt.yaml` |
| `- id: gitter-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/gitter-access-token.yaml` |
| `- id: gocardless-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/gocardless-api-token.yaml` |
| `google-maps-apikeyleak` | `MEDIUM` | 코드에서 잠재적인 Google Maps API 키를 감지합니다. | `generic/security/google-maps-apikeyleak.yaml` |
| `- id: grafana-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/grafana-api-key.yaml` |
| `- id: grafana-cloud-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/grafana-cloud-api-token.yaml` |
| `- id: grafana-service-account-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/grafana-service-account-token.yaml` |
| `- id: harness-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/harness-api-key.yaml` |
| `- id: hashicorp-tf-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/hashicorp-tf-api-token.yaml` |
| `- id: hashicorp-tf-password` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/hashicorp-tf-password.yaml` |
| `- id: heroku-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/heroku-api-key.yaml` |
| `- id: hubspot-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/hubspot-api-key.yaml` |
| `- id: huggingface-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/huggingface-access-token.yaml` |
| `- id: huggingface-organization-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/huggingface-organization-api-token.yaml` |
| `- id: infracost-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/infracost-api-token.yaml` |
| `- id: intercom-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/intercom-api-key.yaml` |
| `- id: intra42-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/intra42-client-secret.yaml` |
| `- id: jfrog-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/jfrog-api-key.yaml` |
| `- id: jfrog-identity-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/jfrog-identity-token.yaml` |
| `- id: jwt` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/jwt.yaml` |
| `- id: jwt-base64` | `INFO` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `generic/gitleaks/jwt-base64.yaml` |
| `- id: kraken-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/kraken-access-token.yaml` |
| `- id: kucoin-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/kucoin-access-token.yaml` |
| `- id: kucoin-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/kucoin-secret-key.yaml` |
| `- id: launchdarkly-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/launchdarkly-access-token.yaml` |
| `- id: linear-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/linear-api-key.yaml` |
| `- id: linear-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/linear-client-secret.yaml` |
| `- id: linkedin-client-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/linkedin-client-id.yaml` |
| `- id: linkedin-client-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/linkedin-client-secret.yaml` |
| `- id: lob-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/lob-api-key.yaml` |
| `- id: lob-pub-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/lob-pub-api-key.yaml` |
| `- id: mailchimp-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/mailchimp-api-key.yaml` |
| `- id: mailgun-private-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/mailgun-private-api-token.yaml` |
| `- id: mailgun-pub-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/mailgun-pub-key.yaml` |
| `- id: mailgun-signing-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/mailgun-signing-key.yaml` |
| `- id: mapbox-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/mapbox-api-token.yaml` |
| `- id: mattermost-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/mattermost-access-token.yaml` |
| `- id: messagebird-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/messagebird-api-token.yaml` |
| `- id: messagebird-client-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/messagebird-client-id.yaml` |
| `- id: microsoft-teams-webhook` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/microsoft-teams-webhook.yaml` |
| `- id: netlify-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/netlify-access-token.yaml` |
| `- id: new-relic-browser-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/new-relic-browser-api-token.yaml` |
| `- id: new-relic-insert-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/new-relic-insert-key.yaml` |
| `- id: new-relic-user-api-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/new-relic-user-api-id.yaml` |
| `- id: new-relic-user-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/new-relic-user-api-key.yaml` |
| `- id: npm-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/npm-access-token.yaml` |
| `- id: nytimes-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/nytimes-access-token.yaml` |
| `- id: okta-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/okta-access-token.yaml` |
| `- id: openai-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/openai-api-key.yaml` |
| `- id: plaid-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/plaid-api-token.yaml` |
| `- id: plaid-client-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/plaid-client-id.yaml` |
| `- id: plaid-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/plaid-secret-key.yaml` |
| `- id: planetscale-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/planetscale-api-token.yaml` |
| `- id: planetscale-oauth-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/planetscale-oauth-token.yaml` |
| `- id: planetscale-password` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/planetscale-password.yaml` |
| `- id: postman-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/postman-api-token.yaml` |
| `- id: prefect-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/prefect-api-token.yaml` |
| `- id: private-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/private-key.yaml` |
| `- id: pulumi-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/pulumi-api-token.yaml` |
| `- id: pypi-upload-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/pypi-upload-token.yaml` |
| `- id: rapidapi-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/rapidapi-access-token.yaml` |
| `- id: readme-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/readme-api-token.yaml` |
| `- id: rubygems-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/rubygems-api-token.yaml` |
| `- id: scalingo-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/scalingo-api-token.yaml` |
| `- id: sendbird-access-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sendbird-access-id.yaml` |
| `- id: sendbird-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sendbird-access-token.yaml` |
| `- id: sendgrid-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sendgrid-api-token.yaml` |
| `- id: sendinblue-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sendinblue-api-token.yaml` |
| `- id: sentry-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sentry-access-token.yaml` |
| `- id: shippo-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/shippo-api-token.yaml` |
| `- id: shopify-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/shopify-access-token.yaml` |
| `- id: shopify-custom-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/shopify-custom-access-token.yaml` |
| `- id: shopify-private-app-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/shopify-private-app-access-token.yaml` |
| `- id: shopify-shared-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/shopify-shared-secret.yaml` |
| `- id: sidekiq-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/sidekiq-secret.yaml` |
| `- id: sidekiq-sensitive-url` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sidekiq-sensitive-url.yaml` |
| `- id: slack-app-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-app-token.yaml` |
| `- id: slack-bot-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-bot-token.yaml` |
| `- id: slack-config-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-config-access-token.yaml` |
| `- id: slack-config-refresh-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-config-refresh-token.yaml` |
| `- id: slack-legacy-bot-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-legacy-bot-token.yaml` |
| `- id: slack-legacy-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-legacy-token.yaml` |
| `- id: slack-legacy-workspace-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-legacy-workspace-token.yaml` |
| `- id: slack-user-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-user-token.yaml` |
| `- id: slack-webhook-url` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/slack-webhook-url.yaml` |
| `- id: snyk-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/snyk-api-token.yaml` |
| `- id: square-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/square-access-token.yaml` |
| `- id: squarespace-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/squarespace-access-token.yaml` |
| `- id: stripe-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/stripe-access-token.yaml` |
| `- id: sumologic-access-id` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sumologic-access-id.yaml` |
| `- id: sumologic-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/sumologic-access-token.yaml` |
| `- id: telegram-bot-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/telegram-bot-api-token.yaml` |
| `- id: travisci-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/travisci-access-token.yaml` |
| `- id: twilio-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/twilio-api-key.yaml` |
| `- id: twitch-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/twitch-api-token.yaml` |
| `- id: twitter-access-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/twitter-access-secret.yaml` |
| `- id: twitter-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/twitter-access-token.yaml` |
| `- id: twitter-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/twitter-api-key.yaml` |
| `- id: twitter-api-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/twitter-api-secret.yaml` |
| `- id: twitter-bearer-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/twitter-bearer-token.yaml` |
| `- id: typeform-api-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/typeform-api-token.yaml` |
| `- id: vault-batch-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/vault-batch-token.yaml` |
| `- id: vault-service-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/vault-service-token.yaml` |
| `- id: yandex-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/yandex-access-token.yaml` |
| `- id: yandex-api-key` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/yandex-api-key.yaml` |
| `- id: yandex-aws-access-token` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/gitleaks/yandex-aws-access-token.yaml` |
| `- id: zendesk-secret-key` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `generic/gitleaks/zendesk-secret-key.yaml` |
## UNICODE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: contains-bidirectional-characters` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `generic/security/bidi.yml` |
## VISUALFORCE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `csp-header-attribute` | `INFO` | csp-header-attribute 잠재적 결함 분기 정비 요망. | `generic/security/ncino/xml/CSPHeaderAttribute.yaml` |
| `use-SRI-for-CDNs` | `WARNING` | use-SRI-for-CDNs 잠재적 결함 분기 정비 요망. | `generic/security/ncino/html/UseSRIForCDNs.yaml` |
| `visualforce-page-api-version` | `WARNING` | visualforce-page-api-version 잠재적 결함 분기 정비 요망. | `generic/security/ncino/xml/VisualForceAPIVersion.yaml` |
| `xss-from-unescaped-url-param` | `ERROR` | xss-from-unescaped-url-param 보안 인젝션 및 런타임 버그 전사 예방 점검. | `generic/security/ncino/vf/XSSFromUnescapedURLParam.yaml` |
# Language: GO

## AWS-LAMBDA

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: database-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `go/security/database-sqli.yaml` |
| `tainted-sql-string` | `ERROR` | Go 보안 및 무결성 관리: tainted-sql-string 점검 처리 하세요. | `go/security/tainted-sql-string.yaml` |
## GORILLA

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: handler-assignment-from-multiple-sources` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/handler-assignment-from-multiple-sources.yaml` |
| `- id: session-cookie-missing-httponly` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `go/security/audit/session-cookie-missing-httponly.yaml` |
| `- id: session-cookie-missing-secure` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `go/security/audit/session-cookie-missing-secure.yaml` |
| `- id: session-cookie-samesitenone` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `go/security/audit/session-cookie-samesitenone.yaml` |
| `- id: websocket-missing-origin-check` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/websocket-missing-origin-check.yaml` |
## GORM

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: gorm-dangerous-method-usage` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `go/security/audit/gorm-dangerous-methods-usage.yaml` |
## GRPC

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `grpc-client-insecure-connection` | `ERROR` | Go 보안 및 무결성 관리: grpc-client-insecure-connection 점검 처리 하세요. | `go/security/grpc-client-insecure-connection.yaml` |
| `grpc-server-insecure-connection` | `ERROR` | Go 보안 및 무결성 관리: grpc-server-insecure-connection 점검 처리 하세요. | `go/security/grpc-server-insecure-connection.yaml` |
## JWT-GO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: hardcoded-jwt-key` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `go/security/jwt.yaml` |
| `jwt-go-none-algorithm` | `ERROR` | Go 보안 및 무결성 관리: jwt-go-none-algorithm 점검 처리 하세요. | `go/security/jwt-none-alg.yaml` |
| `- id: jwt-go-parse-unverified` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `go/security/audit/jwt-parse-unverified.yaml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: avoid-bind-to-all-interfaces` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/bind_all.yaml` |
| `- id: avoid-ssh-insecure-ignore-host-key` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `go/security/audit/crypto/insecure_ssh.yaml` |
| `- id: bad-tmp-file-creation` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/bad_tmp.yaml` |
| `channel-guarded-with-mutex` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/best-practice/channel-guarded-with-mutex.yaml` |
| `- id: cookie-missing-httponly` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `go/security/audit/net/cookie-missing-httponly.yaml` |
| `- id: cookie-missing-secure` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `go/security/audit/net/cookie-missing-secure.yaml` |
| `dangerous-command-write` | `ERROR` | Go 보안 및 무결성 관리: dangerous-command-write 점검 처리 하세요. | `go/security/audit/dangerous-command-write.yaml` |
| `dangerous-exec-cmd` | `ERROR` | Go 보안 및 무결성 관리: dangerous-exec-cmd 점검 처리 하세요. | `go/security/audit/dangerous-exec-cmd.yaml` |
| `dangerous-exec-command` | `ERROR` | Go 보안 및 무결성 관리: dangerous-exec-command 점검 처리 하세요. | `go/security/audit/dangerous-exec-command.yaml` |
| `dangerous-syscall-exec` | `ERROR` | Go 보안 및 무결성 관리: dangerous-syscall-exec 점검 처리 하세요. | `go/security/audit/dangerous-syscall-exec.yaml` |
| `- id: dynamic-httptrace-clienttrace` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/dynamic-httptrace-clienttrace.yaml` |
| `eqeq-is-bad` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/correctness/useless-eqeq.yaml` |
| `exported_loop_pointer` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/correctness/looppointer.yaml` |
| `filepath-clean-misuse` | `ERROR` | Go 보안 및 무결성 관리: filepath-clean-misuse 점검 처리 하세요. | `go/security/filepath-clean-misuse.yaml` |
| `- id: formatted-template-string` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/formatted-template-string.yaml` |
| `- id: fs-directory-listing` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/fs-directory-listing.yaml` |
| `go-unsafe-deserialization-interface` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/deserialization/unsafe-deserialization-interface.yaml` |
| `gosql-sqli` | `ERROR` | Go Lang 다이렉트 SQL 쿼리 가압 단의 인젝션 우려 탑승. | `go/security/audit/sqli/gosql-sqli.yaml` |
| `hidden-goroutine` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/best-practice/hidden-goroutine.yaml` |
| `- id: import-text-template` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/import-text-template.yaml` |
| `incorrect-default-permission` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/correctness/permissions/file_permission.yaml` |
| `- id: insecure-module-used` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `go/security/audit/crypto/bad_imports.yaml` |
| `integer-overflow-int16` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/correctness/overflow/overflow.yaml` |
| `- id: math-random-used` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/crypto/math_random.yaml` |
| `- id: md5-used-as-password` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/md5-used-as-password.yaml` |
| `- id: missing-ssl-minversion` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/crypto/missing-ssl-minversion.yaml` |
| `- id: no-direct-write-to-responsewriter` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/no-direct-write-to-responsewriter.yaml` |
| `- id: no-fprintf-to-responsewriter` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/no-fprintf-to-responsewriter.yaml` |
| `- id: no-interpolation-in-tag` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/no-interpolation-in-tag.yaml` |
| `- id: no-interpolation-js-template-string` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/no-interpolation-js-template-string.yaml` |
| `- id: no-io-writestring-to-responsewriter` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/no-io-writestring-to-responsewriter.yaml` |
| `- id: no-printf-in-responsewriter` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/no-printf-in-responsewriter.yaml` |
| `open-redirect` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/injection/open-redirect.yaml` |
| `- id: parsing-external-entities-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xxe/parsing-external-entities-enabled.yaml` |
| `- id: path-traversal-inside-zip-extraction` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/zip.yaml` |
| `pg-orm-sqli` | `ERROR` | Go Lang 다이렉트 SQL 쿼리 가압 단의 인젝션 우려 탑승. | `go/security/audit/sqli/pg-orm-sqli.yaml` |
| `pg-sqli` | `ERROR` | Go Lang 다이렉트 SQL 쿼리 가압 단의 인젝션 우려 탑승. | `go/security/audit/sqli/pg-sqli.yaml` |
| `pgx-sqli` | `ERROR` | Go Lang 다이렉트 SQL 쿼리 가압 단의 인젝션 우려 탑승. | `go/security/audit/sqli/pgx-sqli.yaml` |
| `- id: potential-dos-via-decompression-bomb` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/decompression_bomb.yaml` |
| `- id: pprof-debug-exposure` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/pprof.yaml` |
| `- id: raw-html-format` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `go/security/injection/raw-html-format.yaml` |
| `reflect-makefunc` | `ERROR` | Go 보안 및 무결성 관리: reflect-makefunc 점검 처리 하세요. | `go/security/audit/reflect-makefunc.yaml` |
| `- id: reverseproxy-director` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/reverseproxy-director.yaml` |
| `- id: sha224-hash` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/crypto/sha224-hash.yaml` |
| `- id: shared-url-struct-mutation` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/shared-url-struct-mutation.yaml` |
| `- id: ssl-v3-is-insecure` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `go/security/audit/crypto/ssl.yaml` |
| `- id: string-formatted-query` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/database/string-formatted-query.yaml` |
| `tainted-sql-string` | `ERROR` | Go 보안 및 무결성 관리: tainted-sql-string 점검 처리 하세요. | `go/security/injection/tainted-sql-string.yaml` |
| `tainted-url-host` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/injection/tainted-url-host.yaml` |
| `- id: tls-with-insecure-cipher` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `go/security/audit/crypto/tls.yaml` |
| `- id: unescaped-data-in-htmlattr` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/unescaped-data-in-htmlattr.yaml` |
| `- id: unescaped-data-in-js` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/unescaped-data-in-js.yaml` |
| `- id: unescaped-data-in-url` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/unescaped-data-in-url.yaml` |
| `- id: unsafe-reflect-by-name` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/unsafe-reflect-by-name.yaml` |
| `- id: unsafe-template-type` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/xss/template-html-does-not-escape.yaml` |
| `use-filepath-join` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/correctness/use-filepath-join.yaml` |
| `- id: use-of-md5` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/crypto/use_of_weak_crypto.yaml` |
| `- id: use-of-unsafe-block` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/unsafe.yaml` |
| `- id: use-of-weak-rsa-key` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/crypto/use_of_weak_rsa_key.yaml` |
| `- id: use-tls` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/use-tls.yaml` |
| `useless-if-conditional` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/maintainability/useless-ifelse.yaml` |
| `- id: wip-xss-using-responsewriter-and-printf` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `go/security/audit/net/wip-xss-using-responsewriter-and-printf.yaml` |
## OTTO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `dangerous-execution` | `ERROR` | Go 보안 및 무결성 관리: dangerous-execution 점검 처리 하세요. | `go/security/audit/dangerous-execution.yaml` |
## TEMPLATE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: go-insecure-templates` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `go/security/insecure-types.yaml` |
| `go-ssti` | `ERROR` | Go 보안 및 무결성 관리: go-ssti 점검 처리 하세요. | `go/security/ssti.yaml` |
# Language: HTML

## BEST-PRACTICE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `robots-denied` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `html/robots-denied.yaml` |
## CORRECTNESS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `https-equiv` | `ERROR` | HTTPS 꼬리표 결여 및 메타 데이터 누출 경하 점검. | `html/https-equiv.yaml` |
## SECURITY

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `eval-detected` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `html/audit/eval-detected.yaml` |
| `- id: insecure-document-method` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `html/audit/insecure-document-method.yaml` |
| `- id: missing-integrity` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `html/audit/missing-integrity.yaml` |
| `- id: plaintext-http-link` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `html/plaintext-http-link.yaml` |
# Language: JAVA

## ANDROID

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `exported_activity` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/android/security/exported_activity.yaml` |
## AWS-LAMBDA

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `tainted-sql-string` | `ERROR` | AWS Lambda 핸들러 등의 외부 사용자 입력이 직접 SQL 문자열 조립에 사용되었습니다. Parameterized Query를 사용하여 SQL 인젝션을 예방하세요. | `java/aws-lambda/security/tainted-sql-string.yaml` |
| `tainted-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/aws-lambda/security/tainted-sqli.yaml` |
## JAVA-JWT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `java-jwt-decode-without-verify` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/java-jwt/security/audit/jwt-decode-without-verify.yaml` |
| `java-jwt-hardcoded-secret` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/java-jwt/security/jwt-hardcode.yaml` |
## JAX-RS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `default-resteasy-provider-abuse` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/jax-rs/security/insecure-resteasy.yaml` |
| `insecure-resteasy-deserialization` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/jax-rs/security/insecure-resteasy.yaml` |
| `jax-rs-path-traversal` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/jax-rs/security/jax-rs-path-traversal.yaml` |
## JBOSS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `seam-log-injection` | `ERROR` | Seam Logging API에 외부 데이터가 그대로 주입되었습니다. 표현식 언어(EL)를 통해 로깅 시스템이 공격받을 수 있으므로 주입 전 엄격한 검증을 통과해야 합니다. | `java/jboss/security/seam-log-injection.yaml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `assignment-comparison` | `ERROR` | 조건식 내부에서 대입 연산자(=)가 사용되고 있습니다. 의도치 않게 할당이 일어나 비교가 고장날 우려가 있습니다. | `java/lang/correctness/assignment-comparison.yaml` |
| `autoescape-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/xss/jsf/autoescape-disabled.yaml` |
| `bad-hexa-conversion` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/bad-hexa-conversion.yaml` |
| `blowfish-insufficient-key-size` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/blowfish-insufficient-key-size.yaml` |
| `cbc-padding-oracle` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/cbc-padding-oracle.yaml` |
| `command-injection-formatted-runtime-call` | `ERROR` | Runtime.exec에 동적 조립 문자열(String.format 등)을 인자로 전달하여 커맨드 인젝션 위험이 있습니다. 안정성을 위해 인자 리스트(String[]) 형식으로 전달하세요. | `java/lang/security/audit/command-injection-formatted-runtime-call.yaml` |
| `command-injection-process-builder` | `ERROR` | ProcessBuilder를 이용한 명령어 가동 시 가변 포매터 및 동적 조립 문자열이 사용되었습니다. 리스트형 인자 전달 스타일로 변경하세요. | `java/lang/security/audit/command-injection-process-builder.yaml` |
| `cookie-missing-httponly` | `WARNING` | 쿠키에 보안 속성(Secure/HttpOnly)이 누락되어 세션 탈취(XSS/MitM)에 노출될 수 있습니다. | `java/lang/security/audit/cookie-missing-httponly.yaml` |
| `cookie-missing-secure-flag` | `WARNING` | 쿠키에 보안 속성(Secure/HttpOnly)이 누락되어 세션 탈취(XSS/MitM)에 노출될 수 있습니다. | `java/lang/security/audit/cookie-missing-secure-flag.yaml` |
| `crlf-injection-logs` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crlf-injection-logs.yaml` |
| `dangerous-groovy-shell` | `WARNING` | Groovy Shell 직접 구동 파사드에 위험 요소 존재할 수 있습니다. | `java/lang/security/audit/dangerous-groovy-shell.yaml` |
| `defaulthttpclient-is-deprecated` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/ssl/defaulthttpclient-is-deprecated.yaml` |
| `desede-is-deprecated` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/desede-is-deprecated.yaml` |
| `do-privileged-use` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/do-privileged-use.yaml` |
| `el-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/el-injection.yaml` |
| `eqeq` | `ERROR` | $X == $X 또는 $X != $X 와 같이 본인 자신을 직접 비교하고 있습니다. 로직 실수이거나 무의미한 연산일 확률이 높습니다. | `java/lang/correctness/eqeq.yaml` |
| `formatted-sql-string` | `ERROR` | SQL 쿼리 내에서 Formatted String(서식 문자열)이 발견되었습니다. SQL 인젝션 요인을 막기 위해 PreparedStatement 스타일을 적용하세요. | `java/lang/security/audit/formatted-sql-string.yaml` |
| `gcm-detection` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/gcm-detection.yaml` |
| `gcm-nonce-reuse` | `ERROR` | GCM 모드 암호화 시 IV(Nonce) 값이 고정으로 재사용되고 있습니다. 암호화 보완성이 즉시 상실되므로 매번 랜덤하고 중복되지 않는 Nonce를 생성하여 주입하십시오. | `java/lang/security/audit/crypto/gcm-nonce-reuse.yaml` |
| `hardcoded-conditional` | `ERROR` | 조건문(if 등)의 조건식이 항상 true 또는 false로 고정되어 의미 없는 연산을 하거나 로직 버그일 가능성이 높습니다. | `java/lang/correctness/hardcoded-conditional.yaml` |
| `hibernate-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/lang/security/audit/sqli/hibernate-sqli.yaml` |
| `http-response-splitting` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/http-response-splitting.yaml` |
| `httpservlet-path-traversal` | `ERROR` | 외부 입력값으로 직접 파일 시스템 경로를 구성해 처리. Relative path 제어 유출을 막기 위해 입력값에 대한 검증을 수행하세요. | `java/lang/security/httpservlet-path-traversal.yaml` |
| `insecure-hostname-verifier` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/ssl/insecure-hostname-verifier.yaml` |
| `insecure-jms-deserialization` | `WARNING` | 안전하지 않은 역직렬화 가동이 포착되었습니다. RCE 공격 차단을 위해 직렬화 파서를 격수하십시오. | `java/lang/security/insecure-jms-deserialization.yaml` |
| `insecure-smtp-connection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/insecure-smtp-connection.yaml` |
| `jackson-unsafe-deserialization` | `WARNING` | 안전하지 않은 역직렬화 가동이 포착되었습니다. RCE 공격 차단을 위해 직렬화 파서를 격수하십시오. | `java/lang/security/jackson-unsafe-deserialization.yaml` |
| `java-pattern-from-string-parameter` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/java-pattern-from-string-parameter.yaml` |
| `java-reverse-shell` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/java-reverse-shell.yaml` |
| `jdbc-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/lang/security/audit/sqli/jdbc-sqli.yaml` |
| `jdo-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/lang/security/audit/sqli/jdo-sqli.yaml` |
| `jpa-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/lang/security/audit/sqli/jpa-sqli.yaml` |
| `ldap-entry-poisoning` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/ldap-entry-poisoning.yaml` |
| `ldap-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/ldap-injection.yaml` |
| `md5-used-as-password` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/md5-used-as-password.yaml` |
| `no-static-initialization-vector` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/no-static-initialization-vector.yaml` |
| `no-string-eqeq` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/correctness/no-string-eqeq.yaml` |
| `object-deserialization` | `WARNING` | 안전하지 않은 역직렬화 가동이 포착되었습니다. RCE 공격 차단을 위해 직렬화 파서를 격수하십시오. | `java/lang/security/audit/object-deserialization.yaml` |
| `ognl-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/ognl-injection.yaml` |
| `overly-permissive-file-permission` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/overly-permissive-file-permission.yaml` |
| `rsa-no-padding` | `WARNING` | Padding 없는 RSA 암호화 기용 시 특정 공격에 무방비하게 노출될 수 있습니다. | `java/lang/security/audit/crypto/rsa-no-padding.yaml` |
| `script-engine-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/script-engine-injection.yaml` |
| `tainted-cmd-from-http-request` | `ERROR` | HTTP 요청 데이터가 exec 또는 ProcessBuilder 명령 인자로 직주입되고 있습니다. 커맨드 인젝션을 격퇴하기 위해 외부 데이터 사용 시 사전에 구성된 인자형 맵 구조로 매핑하세요. | `java/lang/security/audit/tainted-cmd-from-http-request.yaml` |
| `tainted-env-from-http-request` | `ERROR` | HTTP 요청이 시스템 커맨드(`exec`)의 환경 변수로 직주입됩니다. 임의 명령 실행 공격을 예방하기 위해 조치하세요. | `java/lang/security/audit/tainted-env-from-http-request.yaml` |
| `tainted-ldapi-from-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/tainted-ldapi-from-http-request.yaml` |
| `tainted-session-from-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/tainted-session-from-http-request.yaml` |
| `tainted-sql-from-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/sqli/tainted-sql-from-http-request.yaml` |
| `tainted-xpath-from-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/tainted-xpath-from-http-request.yaml` |
| `turbine-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/lang/security/audit/sqli/turbine-sqli.yaml` |
| `unencrypted-socket` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/unencrypted-socket.yaml` |
| `unsafe-reflection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/unsafe-reflection.yaml` |
| `unvalidated-redirect` | `WARNING` | 오픈 리디렉션 피싱 가능성이 존재합니다. 도약 전 검증을 거치세요. | `java/lang/security/audit/unvalidated-redirect.yaml` |
| `url-rewriting` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/url-rewriting.yaml` |
| `use-of-md5-digest-utils` | `WARNING` | 취약한 해시 알고리즘(MD5) 사용이 식별되었습니다. 보안 무결성을 위해 SHA-256 이상을 고르세요. | `java/lang/security/audit/crypto/use-of-md5-digest-utils.yaml` |
| `use-of-md5` | `WARNING` | 취약한 해시 알고리즘(MD5) 사용이 식별되었습니다. 보안 무결성을 위해 SHA-256 이상을 고르세요. | `java/lang/security/audit/crypto/use-of-md5.yaml` |
| `use-of-sha1` | `WARNING` | 취약한 해시 알고리즘(SHA-1)이 포획되었습니다. 대체 보상 처리가 요구됩니다. | `java/lang/security/audit/crypto/use-of-sha1.yaml` |
| `use-of-sha224` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/use-of-sha224.yaml` |
| `use-of-weak-rsa-key` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/audit/crypto/weak-rsa.yaml` |
| `use-snakeyaml-constructor` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/use-snakeyaml-constructor.yaml` |
| `vertx-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/lang/security/audit/sqli/vertx-sqli.yaml` |
| `weak-random` | `WARNING` | 예측 가능한 난수 생성기(Random) 사용 감지. 보안용으론 SecureRandom을 쓰세요. | `java/lang/security/audit/crypto/weak-random.yaml` |
| `weak-ssl-context` | `WARNING` | 취약하거나 검증되지 않은 SSL/TLS 컨텍스트가 감지되었습니다. 중간자 공격(MitM)을 조심하십시오. | `java/lang/security/audit/weak-ssl-context.yaml` |
| `xmlinputfactory-external-entities-enabled` | `ERROR` | XMLInputFactory에 외부 엔티티(External Entities)가 허용되어 있습니다. XML External Entity (XXE) 취약점 발생을 예방하기 위해 이를 비활성화하세요. | `java/lang/security/xmlinputfactory-external-entities-enabled.yaml` |
| `xmlinputfactory-possible-xxe` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/lang/security/xmlinputfactory-possible-xxe.yaml` |
## MONGODB

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `mongodb-nosqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/mongodb/security/injection/audit/mongodb-nosqli.yaml` |
## RMI

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `server-dangerous-class-deserialization` | `WARNING` | 안전하지 않은 역직렬화 가동이 포착되었습니다. RCE 공격 차단을 위해 직렬화 파서를 격수하십시오. | `java/rmi/security/server-dangerous-class-deserialization.yaml` |
| `server-dangerous-object-deserialization` | `ERROR` | RMI 통신 시 임의 객체 수신으로 인한 안전하지 않은 역직렬화 취약점이 가중될 수 있습니다. 허용 클래스 필터링 등을 적용하세요. | `java/rmi/security/server-dangerous-object-deserialization.yaml` |
## SERVLETS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `security-constraint-http-method` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/servlets/security/security-constraint-http-method.yaml` |
## SPRING

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `spel-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/spring/security/audit/spel-injection.yaml` |
| `spring-actuator-fully-enabled-yaml` | `WARNING` | Spring Boot Actuator 엔드포인트가 과도하게 활성화되어 정보 누출 우려가 있습니다. | `java/spring/security/audit/spring-actuator-fully-enabled-yaml.yaml` |
| `spring-actuator-fully-enabled` | `ERROR` | Spring Boot Actuator의 모든 엔드포인트가 완전 개방되어 민감 정보가 노출될 수 있습니다. 필요한 엔티티만 인가하여 사용하세요. | `java/spring/security/audit/spring-actuator-fully-enabled.yaml` |
| `spring-csrf-disabled` | `WARNING` | Spring Security에서 CSRF 보안이 해제되었습니다. | `java/spring/security/audit/spring-csrf-disabled.yaml` |
| `spring-jsp-eval` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/spring/security/audit/spring-jsp-eval.yaml` |
| `spring-sqli` | `WARNING` | 잠재적 SQL 인젝션 취약점이 감지되었습니다. Parameterized Query나 ORM 모델을 준행하세요. | `java/spring/security/audit/spring-sqli.yaml` |
| `spring-unvalidated-redirect` | `WARNING` | 오픈 리디렉션 피싱 가능성이 존재합니다. 도약 전 검증을 거치세요. | `java/spring/security/audit/spring-unvalidated-redirect.yaml` |
| `tainted-file-path` | `ERROR` | 사용자 입력이 직접 파일 제어 경로 상수로 사용됩니다. Path Traversal 등 디렉토리 도약 시도를 검증하고 소거하세요. | `java/spring/security/injection/tainted-file-path.yaml` |
| `tainted-html-string` | `ERROR` | 동적 HTML 주입 연산 시 외부 오염 유입 데이터가 전달되었습니다. XSS 보호막 무력화를 예방하기 위해 사전 이스케이프해야 합니다. | `java/spring/security/injection/tainted-html-string.yaml` |
| `tainted-url-host` | `ERROR` | URL 동적 문자열 조립 시 외부 오염 테두리 호스트 값이 주입됩니다. Open Redirect나 SSRF 위험이 있으니 주소를 정적 고정하거나 검증하세요. | `java/spring/security/injection/tainted-url-host.yaml` |
| `unrestricted-request-mapping` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `java/spring/security/unrestricted-request-mapping.yaml` |
# Language: JAVASCRIPT

## AJV

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `ajv-allerrors-true` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/ajv-allerrors-true.yaml` |
## ANGULAR

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-angular-element-methods` | `INFO` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-element-methods.yaml` |
| `detect-angular-element-taint` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-element-taint.yaml` |
| `detect-angular-open-redirect` | `ERROR` | $window.location.href 연산에 사용자 입력이 그대로 대입되어 오픈 리디렉션 위협이 도사립니다. 도메인 화이트리스트 검출 후 도약시키세요. | `javascript/security/detect-angular-open-redirect.yaml` |
| `detect-angular-resource-loading` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-resource-loading.yaml` |
| `detect-angular-sce-disabled` | `ERROR` | $sceProvider가 false로 설정되어 엄격한 컨텍스트 이스케이프가 비활성화되었습니다. XSS 공격 방어를 위해 SCE를 활성화해야 합니다. | `javascript/security/detect-angular-sce-disabled.yaml` |
| `detect-angular-translateprovider-translations-method` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-third-party-angular-translate.yaml` |
| `detect-angular-trust-as-css-method` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-trust-as-css.yaml` |
| `detect-angular-trust-as-html-method` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-trust-as-html-method.yaml` |
| `detect-angular-trust-as-js-method` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-trust-as-js-method.yaml` |
| `detect-angular-trust-as-method` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-trust-as-method.yaml` |
| `detect-angular-trust-as-resourceurl-method` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-trust-as-resourceurl-method.yaml` |
| `detect-angular-trust-as-url-method` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `javascript/security/detect-angular-trust-as-url-method.yaml` |
## APOLLO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `apollo-axios-ssrf` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/apollo-axios-ssrf.yaml` |
## ARGON2

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `unsafe-argon2-config` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/unsafe-argon2-config.yaml` |
## AUDIT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-replaceall-sanitization` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/detect-replaceall-sanitization.yaml` |
## AWS-LAMBDA

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-child-process` | `ERROR` | child_process를 통한 하위 프로세스 생성 시 임의 인자가 탑재될 위험이 있습니다. 실행 코더와 인자를 고정 배열 스타일로 전달하세요. | `javascript/security/detect-child-process.yaml` |
| `dynamodb-request-object` | `ERROR` | DynamoDB 쿼리 매개변수 집계 시 $EVENT 유입 데이터가 소요됩니다. NoSQL 인젝션을 피하기 위해 입력 데이터를 정적 한정시키세요. | `javascript/security/dynamodb-request-object.yaml` |
| `knex-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/knex-sqli.yaml` |
| `mysql-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/mysql-sqli.yaml` |
| `pg-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/pg-sqli.yaml` |
| `sequelize-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/sequelize-sqli.yaml` |
| `tainted-eval` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/tainted-eval.yaml` |
| `tainted-html-response` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `javascript/security/tainted-html-response.yaml` |
| `tainted-html-string` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `javascript/security/tainted-html-string.yaml` |
| `tainted-sql-string` | `ERROR` | Lambda 가동 연산 스택에 유저 기반 SQL 문자열 조립이 탐지되었습니다. Parameterized Query를 사용하여 인젝션 요인을 차단하십시오. | `javascript/security/tainted-sql-string.yaml` |
| `vm-runincontext-injection` | `ERROR` | vm.runInContext() 호출에 외부 입력 데이터가 유입되어 샌드박스 우회 위험이 있습니다. 가변 코드 실행 연산을 중단하세요. | `javascript/security/vm-runincontext-injection.yaml` |
## BLUEBIRD

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `tofastproperties-code-execution` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/tofastproperties-code-execution.yaml` |
## BROWSER

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `dom-based-xss` | `ERROR` | URL 쿼리나 해시 데이터가 이스케이프 없이 화면 렌더링에 전사됩니다. DOM 기반 XSS 예방을 위해 주입 전 스크립트 특문을 정적 필터링하세... | `javascript/security/dom-based-xss.yaml` |
| `eval-detected` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/eval-detected.yaml` |
| `insecure-document-method` | `ERROR` | document.write 또는 innerHTML 에 외부 오염 데이터가 탑재되어 DOM-XSS 위험이 보입니다. 안전한 DOM 트리 생성 메서드로 변경하세요. | `javascript/security/insecure-document-method.yaml` |
| `insecure-innerhtml` | `ERROR` | $EL.innerHTML에 사용자 제어 데이터가 대입되고 있습니다. XSS 취약점을 유발하는 안티 패턴이므로 textContent 대체 사용 등을 고려하세요. | `javascript/security/insecure-innerhtml.yaml` |
| `insufficient-postmessage-origin-validation` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/insufficient-postmessage-origin-validation.yaml` |
| `js-open-redirect` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/open-redirect.yaml` |
| `js-open-redirect-from-function` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/open-redirect-from-function.yaml` |
| `raw-html-concat` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `javascript/security/raw-html-concat.yaml` |
| `raw-html-join` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `javascript/security/raw-html-join.yaml` |
| `wildcard-postmessage-configuration` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/wildcard-postmessage-configuration.yaml` |
## CHROME-REMOTE-INTERFACE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `chrome-remote-interface-compilescript-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/chrome-remote-interface-compilescript-injection.yaml` |
## DENO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `deno-dangerous-run` | `ERROR` | Deno.run() 호출 시 동적 문자열이 사용되었습니다. 명령어나 인자가 조작되어 커맨드 인젝션(Command Injection)이 발생할 수 있으므로 고정 배열 스타일을 적용하세요. | `javascript/security/audit/deno-dangerous-run.yaml` |
## EXPRESS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `cors-misconfiguration` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/cors-misconfiguration.yaml` |
| `direct-response-write` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/direct-response-write.yaml` |
| `escape-function-overwrite` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/mustache/escape-function-overwrite.yaml` |
| `express-check-csurf-middleware-usage` | `INFO` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-check-csurf-middleware-usage.yaml` |
| `express-check-directory-listing` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-check-directory-listing.yaml` |
| `express-cookie-session-default-name` | `WARNING` | Don’t use the default session cookie name Using the default session cookie name ... | `javascript/security/audit/express-cookie-settings.yaml` |
| `express-cookie-session-default-name` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려가 있습니다. | `javascript/security/audit/express-cookie-settings.yaml` |
| `express-cookie-session-default-name` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려가 있습니다. | `javascript/security/audit/express-cookie-settings.yaml` |
| `express-cookie-session-default-name` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려가 있습니다. | `javascript/security/audit/express-cookie-settings.yaml` |
| `express-cookie-session-default-name` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려가 있습니다. | `javascript/security/audit/express-cookie-settings.yaml` |
| `express-cookie-session-default-name` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려가 있습니다. | `javascript/security/audit/express-cookie-settings.yaml` |
| `express-data-exfiltration` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/express-data-exfiltration.yaml` |
| `express-detect-notevil-usage` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-detect-notevil-usage.yaml` |
| `express-expat-xxe` | `ERROR` | expat XML 파서 구동 시 외부 입력에 검증이 부실합니다. XML External Entity (XXE) 취약점 격출을 무마하기 위해 보안 옵션을 비활성화 처리하십시오. | `javascript/security/express-expat-xxe.yaml` |
| `express-insecure-template-usage` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/express-insecure-template-usage.yaml` |
| `express-jwt-hardcoded-secret` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `javascript/security/express-jwt-hardcoded-secret.yaml` |
| `express-jwt-not-revoked` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `javascript/security/audit/express-jwt-not-revoked.yaml` |
| `express-libxml-noent` | `ERROR` | libxml 라이브러리 처리 시 noent(Entity expansion) 속성이 true 로 인가되었습니다. XXE 취약점에 전방 노출되므로 이를 비활성화 처리하세요. | `javascript/security/audit/express-libxml-noent.yaml` |
| `express-libxml-vm-noent` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-libxml-vm-noent.yaml` |
| `express-open-redirect` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-open-redirect.yaml` |
| `express-path-join-resolve-traversal` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-path-join-resolve-traversal.yaml` |
| `express-phantom-injection` | `ERROR` | phantom API 호출에 사용자 통제 입력 데이터가 결합되었습니다. SSRF 및 인젝션 예방을 위해 사전에 검증된 정적 화이트리스트 맵을 구비하세요. | `javascript/security/express-phantom-injection.yaml` |
| `express-puppeteer-injection` | `ERROR` | puppeteer 인스턴스 메서드 구동 영역에 외부 동적 데이터가 도출됩니다. SSRF 유도 및 원격 클라이언트 브라우저 구동 조작을 차단하십시오. | `javascript/security/express-puppeteer-injection.yaml` |
| `express-res-sendfile` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-res-sendfile.yaml` |
| `express-sandbox-code-injection` | `ERROR` | sandbox 관련 호출에 사용자 입력 데이터가 도달했습니다. 샌드박스 내부에서 임의 코드가 실행될 수 있으므로 동적 조립을 배제하세요. | `javascript/security/express-sandbox-injection.yaml` |
| `express-session-hardcoded-secret` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-session-hardcoded-secret.yaml` |
| `express-ssrf` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-ssrf.yaml` |
| `express-third-party-object-deserialization` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-third-party-object-deserialization.yaml` |
| `express-vm-injection` | `ERROR` | Node.js vm 모듈 스크립트 실행 스택 등에 동적 사용자 오염 테두리가 가압되는 피로도를 잡습니다. 샌드박스 우회를 우려해 사전에 분리 차단하세요. | `javascript/security/express-vm-injection.yaml` |
| `express-vm2-injection` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/express-vm2-injection.yaml` |
| `express-wkhtmltoimage-injection` | `ERROR` | wkhtmltopdf 변환 연산 스택에 동적 오염 인자가 포진했습니다. SSRF와 파괴 인자 인젝션 예방을 위해 입력 주소를 필터링하십시오. | `javascript/security/express-wkhtml-injection.yaml` |
| `express-wkhtmltoimage-injection` | `ERROR` | wkhtmltopdf 변환 연산 스택에 동적 오염 인자가 포진했습니다. SSRF와 파괴 인자 인젝션 예방을 위해 입력 주소를 필터링하십시오. | `javascript/security/express-wkhtml-injection.yaml` |
| `express-xml2json-xxe` | `ERROR` | XML Parser로 입력되는 데이터 가공 시 외부 사용자 입력이 유출됩니다. XXE(XML External Entity) 공격 취약점을 유발하므로 파서에 DTD 로딩 및 외부 엔티티 호출을 차단하세요. | `javascript/security/express-xml2json-xxe.yaml` |
| `express-xml2json-xxe-event` | `WARNING` | Express 프레임워크 라우팅 및 리디렉션 보안 수사. | `javascript/security/audit/express-xml2json-xxe-event.yaml` |
| `raw-html-format` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `javascript/security/injection/raw-html-format.yaml` |
| `remote-property-injection` | `ERROR` | 대괄호 표기법([])에 사용자 입력이 반영되어 객체 속성에 접근하고 있습니다. 프로퍼티 인젝션 및 프로토타입 오염 위험이 있으니 입력값을 사전에 검증하세요. | `javascript/security/audit/remote-property-injection.yaml` |
| `require-request` | `ERROR` | require() 인자 위치에 동적 외부 입력이 발견되었습니다. 예상치 못한 스크립트/모듈 로딩 예방을 위해 정적 모듈 경로로 고정하십시오. | `javascript/security/require-request.yaml` |
| `res-render-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/res-render-injection.yaml` |
| `tainted-sql-string` | `ERROR` | 동적 SQL 문자열 결합이 탐지되었습니다. SQL Injection 예방을 위해 객체 모델 바인딩 및 Parameterized Query 기법을 사용하세요. | `javascript/security/injection/tainted-sql-string.yaml` |
| `template-and-attributes` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/pug/and-attributes.yaml` |
| `template-explicit-unescape` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/pug/explicit-unescape.yaml` |
| `template-explicit-unescape` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/ejs/explicit-unescape.yaml` |
| `template-explicit-unescape` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/mustache/explicit-unescape.yaml` |
| `unknown-value-in-redirect` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/possible-user-input-redirect.yaml` |
| `var-in-href` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/pug/var-in-href.yaml` |
| `var-in-href` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/ejs/var-in-href.yaml` |
| `var-in-script-src` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/ejs/var-in-script-src.yaml` |
| `var-in-script-tag` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/pug/var-in-script-tag.yaml` |
| `var-in-script-tag` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/ejs/var-in-script-tag.yaml` |
| `var-in-script-tag` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/mustache/var-in-script-tag.yaml` |
| `x-frame-options-misconfiguration` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/x-frame-options-misconfiguration.yaml` |
## FBJS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `insecure-createnodesfrommarkup` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/insecure-createnodesfrommarkup.yaml` |
## GRPC

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `grpc-nodejs-insecure-connection` | `ERROR` | gRPC 연결 생성 시 암호화되지 않은 안전하지 않은 채널(createInsecure())이 사용되었습니다. 중간자 공격(MitM) 등에 무방비하므로 SSL/TLS 보완책을 적용하십시오. | `javascript/security/grpc-nodejs-insecure-connection.yaml` |
## INTERCOM

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `intercom-settings-user-identifier-without-user-hash` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/intercom-settings-user-identifier-without-user-hash.yaml` |
## JOSE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `hardcoded-jwt-secret` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `javascript/security/jwt-hardcode.yaml` |
| `jose-exposed-data` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/jose-exposed-data.yaml` |
| `jwt-none-alg` | `ERROR` | jose 라이브러리 연산에서 서명 해제용 none 알고리즘 가압이 포착되었습니다. 변조 토큰 패싱을 예방하기 위해 보안 고정 알고리즘을 사용하십시오. | `javascript/security/jwt-none-alg.yaml` |
## JQUERY

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `jquery-insecure-method` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/jquery-insecure-method.yaml` |
| `jquery-insecure-selector` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/jquery-insecure-selector.yaml` |
| `prohibit-jquery-html` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/prohibit-jquery-html.yaml` |
## JSONWEBTOKEN

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `hardcoded-jwt-secret` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `javascript/security/jwt-hardcode.yaml` |
| `jwt-decode-without-verify` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `javascript/security/audit/jwt-decode-without-verify.yaml` |
| `jwt-exposed-data` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `javascript/security/audit/jwt-exposed-data.yaml` |
| `jwt-none-alg` | `ERROR` | jsonwebtoken 서명 알고리즘에 검증 해제용 none 알고리즘 가압이 포착되었습니다. 변조 토큰 인용을 방지하기 위해 확실한 암호화 알고리즘만 허용하도록 고정하세요. | `javascript/security/jwt-none-alg.yaml` |
## JWT-SIMPLE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `jwt-simple-noverify` | `ERROR` | jwt-simple 디코딩 수동 호출 시 서명 검증(verify) 단계가 누락되었습니다. 변조 토큰 우회를 막기 위해 반드시 검증 옵션을 활성화하세요. | `javascript/security/jwt-simple-noverify.yaml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `assigned-undefined` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/best-practice/assigned-undefined.yaml` |
| `code-string-concat` | `ERROR` | 요청 데이터를 eval() 또는 Function() 실행 인자로 직접 연산 합사합니다. 원격 코드 실행(RCE) 타격을 유발하므로 동적 코드 실행을 금지하십시오. | `javascript/security/audit/code-string-concat.yaml` |
| `dangerous-spawn-shell` | `ERROR` | 비 리터럴 문자열을 실행 명령 매개체로 탑재하는 위험 spawn 구조를 감지했습니다. 명령 실행의 안정성을 위해 정적 리스트를 적용하세요. | `javascript/security/audit/dangerous-spawn-shell.yaml` |
| `detect-buffer-noassert` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/detect-buffer-noassert.yaml` |
| `detect-child-process` | `ERROR` | child_process 범용 호출 시 유입 인자의 정적 분기가 누락되었습니다. 커맨드 스페이스 조작에 의한 인젝션 방지를 위해 규격 배열을 고수하세요. | `javascript/security/detect-child-process.yaml` |
| `detect-disable-mustache-escape` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/detect-disable-mustache-escape.yaml` |
| `detect-eval-with-expression` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/detect-eval-with-expression.yaml` |
| `detect-insecure-websocket` | `ERROR` | 보안되지 않은 WebSocket(ws://) 연결이 감지되었습니다. 스니핑 공격을 예방하기 위해 암호화된 wss:// 연결을 사용하세요. | `javascript/security/detect-insecure-websocket.yaml` |
| `detect-no-csrf-before-method-override` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/detect-no-csrf-before-method-override.yaml` |
| `detect-non-literal-fs-filename` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/detect-non-literal-fs-filename.yaml` |
| `detect-non-literal-regexp` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/detect-non-literal-regexp.yaml` |
| `detect-non-literal-require` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/detect-non-literal-require.yaml` |
| `detect-pseudoRandomBytes` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/detect-pseudoRandomBytes.yaml` |
| `detect-redos` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/detect-redos.yaml` |
| `eqeq-is-bad` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/correctness/useless-eqeq.yaml` |
| `hardcoded-hmac-key` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/hardcoded-hmac-key.yaml` |
| `html-in-template-string` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `javascript/security/html-in-template-string.yaml` |
| `incomplete-sanitization` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/incomplete-sanitization.yaml` |
| `insecure-object-assign` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/insecure-object-assign.yaml` |
| `javascript-alert` | `WARNING` | 프로덕션 코드에 `prompt()` 호출이 남아있습니다. 배포 전 반드시 제거해 주세요. | `javascript/best-practice/leftover_debugging.yaml` |
| `javascript-alert` | `WARNING` | 프로덕션 코드에 `prompt()` 호출이 남아있습니다. 배포 전 반드시 제거해 주세요. | `javascript/best-practice/leftover_debugging.yaml` |
| `javascript-alert` | `WARNING` | 프로덕션 코드에 `prompt()` 호출이 남아있습니다. 배포 전 반드시 제거해 주세요. | `javascript/best-practice/leftover_debugging.yaml` |
| `javascript-alert` | `WARNING` | 프로덕션 코드에 `prompt()` 호출이 남아있습니다. 배포 전 반드시 제거해 주세요. | `javascript/best-practice/leftover_debugging.yaml` |
| `lazy-load-module` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/best-practice/lazy-load-module.yaml` |
| `md5-used-as-password` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/md5-used-as-password.yaml` |
| `missing-template-string-indicator` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/correctness/missing-template-string-indicator.yaml` |
| `no-replaceall` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/correctness/no-replaceall.yaml` |
| `no-stringify-keys` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/correctness/no-stringify-keys.yaml` |
| `node-knex-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/audit/sqli/node-knex-sqli.yaml` |
| `node-mssql-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/audit/sqli/node-mssql-sqli.yaml` |
| `node-mysql-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/audit/sqli/node-mysql-sqli.yaml` |
| `node-postgres-sqli` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `javascript/security/audit/sqli/node-postgres-sqli.yaml` |
| `path-join-resolve-traversal` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/path-traversal/path-join-resolve-traversal.yaml` |
| `prototype-pollution-assignment` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/prototype-pollution/prototype-pollution-assignment.yaml` |
| `prototype-pollution-loop` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/prototype-pollution/prototype-pollution-loop.yaml` |
| `spawn-git-clone` | `ERROR` | git clone 실행 인자에 사용자 오염 가변 데이터가 유입되었습니다. 인자 인젝션 위험이 있으므로 주소를 고정하거나 정적 세탁하세요. | `javascript/security/spawn-git-clone.yaml` |
| `spawn-shell-true` | `ERROR` | spawn 구동 시 {shell: true} 옵션이 사용되었습니다. 인자 하이재킹에 취약하므로 옵션을 철회하고 배열형 인자를 명시하세요. | `javascript/security/audit/spawn-shell-true.yaml` |
| `unknown-value-with-script-tag` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/unknown-value-with-script-tag.yaml` |
| `unsafe-dynamic-method` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/unsafe-dynamic-method.yaml` |
| `unsafe-formatstring` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/unsafe-formatstring.yaml` |
| `useless-assignment` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/correctness/useless-assign.yaml` |
| `zlib-async-loop` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/best-practice/zlib-async-loop.yaml` |
## MONACO-EDITOR

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `monaco-hover-htmlsupport` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/monaco-hover-htmlsupport.yaml` |
## NODE-CRYPTO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `aead-no-final` | `ERROR` | AEAD 암호 해독 객체의 final() 호출이 빠졌습니다. 인증 태그(Tag) 검증이 스킵되어 위조 데이터 분별이 안되므로 필수 호출하세요. | `javascript/security/aead-no-final.yaml` |
| `create-de-cipher-no-iv` | `ERROR` | 지원 중단된 createCipher 기용이 잡혔습니다. 암호화 해독 위험이 크므로 createCipheriv 로 고유 IV를 주입하여 보강하세요. | `javascript/security/create-de-cipher-no-iv.yaml` |
| `gcm-no-tag-length` | `ERROR` | GCM 모드 구동 시 createDecipheriv 에 인증 태그(Tag) 탑재 확인 옵션이 누락되었습니다. 데이터 무결성 검증을 위해 반드시 활성화하세요. | `javascript/security/gcm-no-tag-length.yaml` |
## NODE-EXPAT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `expat-xxe` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/expat-xxe.yaml` |
## PASSPORT-JWT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `hardcoded-passport-secret` | `WARNING` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `javascript/security/passport-hardcode.yaml` |
## PHANTOM

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `phantom-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/phantom-injection.yaml` |
## PLAYWRIGHT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `playwright-addinitscript-code-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/playwright-addinitscript-code-injection.yaml` |
| `playwright-evaluate-arg-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/playwright-evaluate-arg-injection.yaml` |
| `playwright-evaluate-code-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/playwright-evaluate-code-injection.yaml` |
| `playwright-exposed-chrome-devtools` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/playwright-exposed-chrome-devtools.yaml` |
| `playwright-goto-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/playwright-goto-injection.yaml` |
| `playwright-setcontent-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/playwright-setcontent-injection.yaml` |
## PUPPETEER

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `puppeteer-evaluate-arg-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/puppeteer-evaluate-arg-injection.yaml` |
| `puppeteer-evaluate-code-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/puppeteer-evaluate-code-injection.yaml` |
| `puppeteer-exposed-chrome-devtools` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/puppeteer-exposed-chrome-devtools.yaml` |
| `puppeteer-goto-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/puppeteer-goto-injection.yaml` |
| `puppeteer-setcontent-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/puppeteer-setcontent-injection.yaml` |
## REACT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `calling-set-state-on-current-state` | `ERROR` | React Hooks에서 현재 상태(state) 값을 그대로 다시 setState에 주입하는 무의미한 연산이 감지되었습니다. 로직 실수를 검토하세요. | `javascript/correctness/hooks/set-state-no-op.yaml` |
## SANDBOX

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `sandbox-code-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/sandbox-code-injection.yaml` |
## SAX

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `sax-xxe` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/sax-xxe.yaml` |
## SEQUELIZE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `express-sequelize-injection` | `ERROR` | Sequelize 바인딩 스택에 인화성 가변 파라미터가 유입됩니다. SQL Injection 방어를 위해 ORM 규격 바인딩(replacements)을 쓰세요. | `javascript/security/audit/sequelize-injection-express.yaml` |
| `sequelize-enforce-tls` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/sequelize-enforce-tls.yaml` |
| `sequelize-raw-query` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/sequelize-raw-query.yaml` |
| `sequelize-tls-disabled-cert-validation` | `ERROR` | Sequelize 설정 중 인증서 검증을 우회(rejectUnauthorized: false)하는 상태가 감지되었습니다. 중간자 공격에 도출되므로 프로덕션 배포 시 철회하십시오. | `javascript/security/audit/sequelize-tls-disabled-cert-validation.yaml` |
| `sequelize-weak-tls-version` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/sequelize-weak-tls-version.yaml` |
## SERIALIZE-JAVASCRIPT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `unsafe-serialize-javascript` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/unsafe-serialize-javascript.yaml` |
## SHELLJS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `shelljs-exec-injection` | `ERROR` | shelljs.exec() 호출 연산에 외부 사용자 오염 가변 데이터가 주입되었습니다. RCE(원격코드실행) 위협이 가중되므로 인자 분할 형식으로 전환하십시오. | `javascript/security/shelljs-exec-injection.yaml` |
## THENIFY

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `multiargs-code-execution` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/multiargs-code-execution.yaml` |
## VM2

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `vm2-code-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/vm2-code-injection.yaml` |
| `vm2-context-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/vm2-context-injection.yaml` |
## VUE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `avoid-v-html` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xss/templates/avoid-v-html.yaml` |
## WKHTMLTOIMAGE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `wkhtmltoimage-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/wkhtmltoimage-injection.yaml` |
## WKHTMLTOPDF

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `wkhtmltopdf-injection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/wkhtmltopdf-injection.yaml` |
## XML2JSON

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `xml2json-xxe` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `javascript/security/audit/xml2json-xxe.yaml` |
# Language: JSON

## AWS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: public-s3-bucket` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `json/security/public-s3-bucket.yaml` |
| `- id: public-s3-policy-statement` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `json/security/public-s3-policy-statement.yaml` |
| `wildcard-assume-role` | `ERROR` | wildcard-assume-role 보안 인젝션 및 런타임 버그 전사 예방 점검. | `json/security/wildcard-assume-role.yaml` |
## NPM

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: package-dependencies-check` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `json/security/package-dependencies-check.yml` |
# Language: KOTLIN

## GRADLE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: build-gradle-password-hardcoded` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/build-gradle-password-hardcoded.yaml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: unencrypted-socket` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/unencrypted-socket.yaml` |
| `- id: use-of-sha1` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/use-of-sha1.yaml` |
| `- id: no-null-cipher` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/no-null-cipher.yaml` |
| `- id: defaulthttpclient-is-deprecated` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/defaulthttpclient-is-deprecated.yaml` |
| `- id: anonymous-ldap-bind` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/anonymous-ldap-bind.yaml` |
| `- id: bad-hexa-conversion` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/bad-hexa-conversion.yaml` |
| `command-injection-formatted-runtime-call` | `ERROR` | java.lang.Runtime 호출 인자에 가변 포맷 또는 동적 합산 문자열이 감지되었습니다. 외부 입력 탑재 시 명령어 주입(Command Injection) 위협이 가중되므로 정적 분할 전달이나 세척을 거치십시오. | `kotlin/security/command-injection-formatted-runtime-call.yaml` |
| `- id: cookie-missing-httponly` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `kotlin/security/cookie-missing-httponly.yaml` |
| `- id: cookie-missing-secure-flag` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `kotlin/security/cookie-missing-secure-flag.yaml` |
| `- id: ecb-cipher` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/ecb-cipher.yaml` |
| `- id: gcm-detection` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/gcm-detection.yaml` |
| `- id: use-of-md5` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/use-of-md5.yaml` |
| `- id: use-of-weak-rsa-key` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `kotlin/security/weak-rsa.yaml` |
# Language: PROBLEM-BASED-PACKS

## INSECURE-TRANSPORT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: bypass-tls-verification` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/bypass-tls-verification.yaml` |
| `- id: bypass-tls-verification` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/bypass-tls-verification.yaml` |
| `- id: bypass-tls-verification` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-spring/bypass-tls-verification.yaml` |
| `- id: bypass-tls-verification` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/bypass-tls-verification.yaml` |
| `- id: disallow-old-tls-versions` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/disallow-old-tls-versions.yaml` |
| `- id: disallow-old-tls-versions1` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/disallow-old-tls-versions1.yaml` |
| `- id: disallow-old-tls-versions1` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/disallow-old-tls-versions1.yaml` |
| `- id: disallow-old-tls-versions2` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/disallow-old-tls-versions2.yaml` |
| `- id: disallow-old-tls-versions2` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/disallow-old-tls-versions2.yaml` |
| `- id: ftp-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/ftp-request.yaml` |
| `- id: ftp-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/ftp-request.yaml` |
| `- id: ftp-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/ftp-request.yaml` |
| `- id: gorequest-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/gorequest-http-request.yaml` |
| `- id: grequests-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/grequests-http-request.yaml` |
| `- id: http-client-requests` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/ruby-stdlib/http-client-requests.yaml` |
| `- id: http-components-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/http-components-request.yaml` |
| `- id: http-customized-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/http-customized-request.yaml` |
| `- id: http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/http-request.yaml` |
| `- id: http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/http-request.yaml` |
| `- id: httpclient-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/httpclient-http-request.yaml` |
| `- id: httpget-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/httpget-http-request.yaml` |
| `- id: httpurlconnection-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/httpurlconnection-http-request.yaml` |
| `- id: net-ftp-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/ruby-stdlib/net-ftp-request.yaml` |
| `- id: net-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/ruby-stdlib/net-http-request.yaml` |
| `- id: net-telnet-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/ruby-stdlib/net-telnet-request.yaml` |
| `- id: openuri-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/ruby-stdlib/openuri-request.yaml` |
| `- id: rest-http-client-support` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/rest-http-client-support.yaml` |
| `- id: sling-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/sling-http-request.yaml` |
| `- id: socket-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/socket-request.yaml` |
| `- id: spring-ftp-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-spring/spring-ftp-request.yaml` |
| `- id: spring-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-spring/spring-http-request.yaml` |
| `- id: telnet-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/telnet-request.yaml` |
| `- id: telnet-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/go-stdlib/telnet-request.yaml` |
| `- id: telnet-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/telnet-request.yaml` |
| `- id: tls-renegotiation` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/tls-renegotiation.yaml` |
| `- id: unirest-http-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/java-stdlib/unirest-http-request.yaml` |
| `- id: using-http-server` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `problem-based-packs/js-node/using-http-server.yaml` |
# Language: PYTHON

## AIRFLOW

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `formatted-string-bashoperator` | `ERROR` | BashOperator 내부 가동 인자에 가변 합사가 포착되었습니다. 외부 주입 우려 노출을 전사 단속하십시오. | `python/security/audit/formatted-string-bashoperator.yaml` |
## ATTR

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `attr-mutable-initializer` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/mutable-initializer.yaml` |
## AWS-LAMBDA

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/pymssql-sqli.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/mysql-sqli.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/tainted-pickle-deserialization.yaml` |
| `-` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `python/security/tainted-html-response.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/pymysql-sqli.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/tainted-code-exec.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/sqlalchemy-sqli.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/psycopg-sqli.yaml` |
| `dangerous-asyncio-create-exec` | `ERROR` | create_subprocess_exec 버퍼에 외부 데이터 합치기가 발견되었습니다. 인젝션 예방을 위해 필터링 하세요. | `python/security/dangerous-asyncio-create-exec.yaml` |
| `dangerous-asyncio-exec` | `ERROR` | subprocess_exec 가동 시 유저 리스트 오염 인자가 흐릅니다. 가변 탑재를 배제하십시오. | `python/security/dangerous-asyncio-exec.yaml` |
| `dangerous-asyncio-shell` | `ERROR` | asyncio 쉘 연산에 가변 인자가 합사되었습니다. 명령어 주입 우려가 있으니 shlex 세척을 권장합니다. | `python/security/dangerous-asyncio-shell.yaml` |
| `dangerous-spawn-process` | `ERROR` | os 바이너리 스폰 가동에 event 가변 오염이 흐릅니다. 쉘 명령어 덤프 RCE 차단을 위해 전사 단속하십시오. | `python/security/dangerous-spawn-process.yaml` |
| `dangerous-subprocess-use` | `ERROR` | subprocess 구동 시 shell=True 탑재 탑재로 보안 결함이 높습니다. 가변 인자 결합을 철회하십시오. | `python/security/dangerous-subprocess-use.yaml` |
| `dangerous-system-call` | `ERROR` | os 모듈 가동에 event 오염 인자가 유입됩니다. 인젝션 위험이 극에 달하므로 subprocess를 고용하세요. | `python/security/dangerous-system-call.yaml` |
| `dynamodb-filter-injection` | `ERROR` | DynamoDB 쿼리 필터에 $EVENT 유입 인자가 도주합니다. NoSQL 인젝션 예방을 위해 정적 할당 하십시오. | `python/security/dynamodb-filter-injection.yaml` |
| `tainted-html-string` | `WARNING` | tainted-html-string 잠재적 결함 분기 정비 요망. | `python/security/tainted-html-string.yaml` |
| `tainted-sql-string` | `ERROR` | Lambda 가동 내 동적 SQL 조립이 탐지되었습니다. Parameterized Query를 사용하여 요인을 차단하십시오. | `python/security/tainted-sql-string.yaml` |
## BOKEH

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `bokeh-deprecated-apis` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/maintainability/deprecated/deprecated_apis.yaml` |
## BOTO3

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/hardcoded-token.yaml` |
## CLICK

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `use-click-secho` | `ERROR` | click.echo()와 style() 수동 배합 대신 click.secho() 연쇄 고용을 권장합니다. | `python/best-practice/echo-style.yaml` |
## CORRECTNESS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/check-is-none-explicitly.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/suppressed-exception-handling-finally-break.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/socket-shutdown-close.yaml` |
## CRYPTOGRAPHY

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/insufficient-rsa-key-size.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/insufficient-ec-key-size.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithms.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-mode-ecb.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithms-blowfish.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithms-arc4.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/insufficient-dsa-key-size.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithms-md5.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/empty-aes-key.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithms.yaml` |
| `crypto-mode-without-authentication` | `ERROR` | 무결성 검증 없는 암호화 운영 모드가 사용되었습니다. GCM 등 AEAD 모드로 교정하세요. | `python/security/mode-without-authentication.yaml` |
## DISTRIBUTED

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security.yaml` |
## DJANGO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/avoid-mark-safe.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/custom-expression-as-sql.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/extends-custom-expression.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/secure-cookies.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/csrf-exempt.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/query-set-extra.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/raw-query.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/unvalidated-password.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/direct-use-of-httpresponse.yaml` |
| `-` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `python/security/audit/xss/formathtml-fstring-parameter.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/template-var-unescaped-with-safeseq.yaml` |
| `-` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `python/security/audit/xss/html-safe.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/class-extends-safestring.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/context-autoescape-off.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/template-autoescape-off.yaml` |
| `-` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `python/security/audit/xss/html-magic-method.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/global-autoescape-off.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/filter-with-is-safe.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/django-rest-framework/missing-throttle-config.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/open-redirect.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/reflected-data-httpresponse.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/tainted-url-host.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/reflected-data-httpresponsebadrequest.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/request-data-fileresponse.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/request-data-write.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/code/globals-misuse-code-execution.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/code/user-eval.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/code/user-exec-format-string.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/code/user-exec.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/code/user-eval-format-string.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/path-traversal/path-traversal-open.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/path-traversal/path-traversal-file-name.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/path-traversal/path-traversal-join.yaml` |
| `-` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `python/security/injection/email/xss-send-mail-html-message.yaml` |
| `-` | `WARNING` | HTML 템플릿 가속 피싱 대응. | `python/security/injection/email/xss-html-email-body.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/sql/sql-injection-extra.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/sql/sql-injection-rawsql.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/sql/sql-injection-using-db-cursor-execute.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/sql/sql-injection-using-raw.yaml` |
| `access-foreign-keys` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/performance/access-foreign-keys.yaml` |
| `avoid-insecure-deserialization` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 avoid-insecure-deserialization 전사 단속이 요구됩니다. | `python/security/audit/avoid-insecure-deserialization.yaml` |
| `command-injection-os-system` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 command-injection-os-system 전사 단속이 요구됩니다. | `python/security/injection/command/command-injection-os-system.yaml` |
| `csv-writer-injection` | `ERROR` | CSV 가동 인자 인젝션 방어를 조율 하십시오. | `python/security/injection/csv-writer-injection.yaml` |
| `debug-template-tag` | `WARNING` | debug-template-tag 잠재적 결함 분기 정비 요망. | `python/security/audit/templates/debug-template-tag.yaml` |
| `django-compat-2_0-signals-weak` | `WARNING` | Django 프레임워크 ORM 및 템플릿 보안 검증. | `python/compatibility/django-2_0-compat.yaml` |
| `django-db-model-save-super` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/model-save.yaml` |
| `django-no-csrf-token` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/django-no-csrf-token.yaml` |
| `django-using-request-post-after-is-valid` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/django-using-request-post-after-is-valid.yaml` |
| `duplicate-path-assignment` | `ERROR` | 서로 다른 경로에 동일한 URL 이름(`$NAME`)이 중복 할당되어 있습니다. 이름 충돌을 방지하기 위해 각 경로에 고유한 이름을 지정해 주세요. | `python/maintainability/duplicate-path-assignment.yaml` |
| `globals-as-template-context` | `ERROR` | globals() 전역 인출 피칭 국면을 경보합니다. | `python/security/globals-as-template-context.yaml` |
| `hashids-with-django-secret` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 hashids-with-django-secret 전사 단속이 요구됩니다. | `python/security/hashids-with-django-secret.yaml` |
| `locals-as-template-context` | `ERROR` | locals() 직접 전사로 내부 탑재 변수가 외부에 탈주 폭주하는 위험 레이어를 점검합니다. | `python/security/locals-as-template-context.yaml` |
| `mass-assignment` | `WARNING` | mass-assignment 잠재적 결함 분기 정비 요망. | `python/security/injection/mass-assignment.yaml` |
| `nan-injection` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 nan-injection 전사 단속이 요구됩니다. | `python/security/nan-injection.yaml` |
| `nontext-field-must-set-null-true` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 nontext-field-must-set-null-true 전사 단속이 요구됩니다. | `python/correctness/nontext-field-must-set-null-true.yaml` |
| `password-empty-string` | `ERROR` | 빈 문자열로 패스워드가 세팅되는 안티 무방비 보안 지점을 진압합니다. | `python/security/passwords/password-empty-string.yaml` |
| `raw-html-format` | `WARNING` | raw-html-format 잠재적 결함 분기 정비 요망. | `python/security/injection/raw-html-format.yaml` |
| `ssrf-injection-requests` | `ERROR` | 서버 측 요청 위조(SSRF) 누출 피싱 주소 교정. | `python/security/injection/ssrf/ssrf-injection-requests.yaml` |
| `ssrf-injection-urllib` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 ssrf-injection-urllib 전사 단속이 요구됩니다. | `python/security/injection/ssrf/ssrf-injection-urllib.yaml` |
| `no-null-string-field` | `ERROR` | unique=True와 blank=True를 함께 선언하는 문자열 필드는 null=True도 반드시 설정해야 합니다. 그렇지 않으면 빈 값으로 여러 레코드를 저장할 때 고유 제약 조건 위반이 발생할 수 있습니다. | `python/correctness/string-field-null-checks.yaml` |
| `subprocess-injection` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 subprocess-injection 전사 단속이 요구됩니다. | `python/security/injection/command/subprocess-injection.yaml` |
| `tainted-sql-string` | `ERROR` | 동적 SQL 문자열 결합 유출이 포착되어 Parameterized 쿼리를 독촉합니다. | `python/security/injection/tainted-sql-string.yaml` |
| `template-blocktranslate-no-escape` | `INFO` | template-blocktranslate-no-escape 잠재적 결함 분기 정비 요망. | `python/security/audit/xss/template-blocktranslate-no-escape.yaml` |
| `template-translate-as-no-escape` | `INFO` | template-translate-as-no-escape 잠재적 결함 분기 정비 요망. | `python/security/audit/xss/template-translate-as-no-escape.yaml` |
| `use-count-method` | `ERROR` | .count() 헬퍼 가동으로 쿼리 집계 가속을 도모 하세요. | `python/performance/upsell-count.yaml` |
| `use-decimalfield-for-money` | `ERROR` | 금액 필드에 부동 소수점 오차를 막기 위해 DecimalField를 사수 하십시오. | `python/correctness/use-decimalfield-for-money.yaml` |
| `use-django-environ` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 use-django-environ 전사 단속이 요구됩니다. | `python/best-practice/upsell_django_environ.yaml` |
| `use-earliest-or-latest` | `ERROR` | use-earliest-or-latest 보안 인젝션 및 런타임 버그 전사 예방 점검. | `python/performance/upsell_earliest_latest.yaml` |
| `use-json-response` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 use-json-response 전사 단속이 요구됩니다. | `python/best-practice/json_response.yaml` |
| `use-none-for-password-default` | `ERROR` | 비밀번호 기본 데이터에 무효 인자 바인딩을 권장합니다. | `python/security/passwords/use-none-for-password-default.yaml` |
| `use-onetoonefield` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/use-onetoonefield.yaml` |
## DOCKER

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/docker-arbitrary-container-run.yaml` |
## FASTAPI

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `wildcard-cors` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/wildcard-cors.yaml` |
## FLASK

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/secure-static-file-serve.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/unsanitized-input.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/unescaped-template-extension.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/xss/audit/direct-use-of-jinja2.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/xss/audit/template-unquoted-attribute-var.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/xss/audit/explicit-unescape-with-markup.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/xss/audit/template-unescaped-with-safe.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/xss/audit/template-autoescape-off.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/debug-enabled.yaml` |
| `-` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/host-header-injection-python.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/app-run-param-config.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/flask-url-for-external-true.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/secure-set-cookie.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/wtf-csrf-disabled.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/app-run-security-config.yaml` |
| `-` | `WARNING` | avoid_hardcoded_config_DEBUG 잠재적 결함 분기 정비 요망. | `python/security/audit/hardcoded-config.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/directly-returned-format-string.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/render-template-string.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/xss/make-response-with-unknown-content.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/injection/tainted-url-host.yaml` |
| `avoid-accessing-request-in-wrong-handler` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/access-request-in-wrong-handler.yaml` |
| `csv-writer-injection` | `ERROR` | 내장 csv 모듈에 검증 실종 동동 데이터를 공급 중입니다. 매크로 주입 방어를 위해 특수 서식 이격을 거치세요. | `python/security/injection/csv-writer-injection.yaml` |
| `dangerous-template-string` | `ERROR` | 가변 스트링 합사를 통한 동적 템플릿 생산이 잡혔습니다. SSTI 예방을 위해 객체 렌더링 양식을 쓰세요. | `python/security/dangerous-template-string.yaml` |
| `eval-injection` | `ERROR` | eval() 연산 버퍼에 가변 사용자 동적 연산 입력이 관여합니다. 원격 코드 실행(RCE) 타격을 유발하므로 즉각 피하십시오. | `python/security/injection/user-eval.yaml` |
| `exec-injection` | `ERROR` | exec() 연산 버퍼에 가변 사용자 동적 인가 데이터가 합사됩니다. RCE 극심한 타격이 상존하니 중지하십시오. | `python/security/injection/user-exec.yaml` |
| `flask-api-method-string-format` | `ERROR` | 컨트롤러 연산에 유저 가변 문자열이 수동 가압됩니다. 인젝션 등 우회 타격 부재를 조심하십시오. | `python/security/flask-api-method-string-format.yaml` |
| `flask-cache-query-string` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/caching/query-string.yaml` |
| `flask-class-method-get-side-effects` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/get-class-method-with-side-effects.yaml` |
| `flask-cors-misconfiguration` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/flask-cors-misconfiguration.yaml` |
| `flask-deprecated-apis` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/maintainability/deprecated/deprecated-apis.yaml` |
| `flask-duplicate-handler-name` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/same-handler-name.yaml` |
| `hashids-with-flask-secret` | `ERROR` | Flask SECRET_KEY를 HashIDs Salt로 오용했습니다. 해시 파싱 중 무력화 위협이 있으니 전용 값을 할당하세요. | `python/security/hashids-with-flask-secret.yaml` |
| `insecure-deserialization` | `ERROR` | 취약한 역직렬화 라이브러리가 감지되었습니다. RCE 공격 위험을 막기 위해 JSON 등 안전한 형태를 이용하세요. | `python/security/insecure-deserialization.yaml` |
| `nan-injection` | `ERROR` | 유저 입력이 typecast(float 등) 안으로 직접 유출됩니다. 데이터 연산 우회 조작이 발견될 수 있으니 검사하세요. | `python/security/injection/nan-injection.yaml` |
| `open-redirect` | `ERROR` | 외부 유입 주소 파라미터가 가공 없이 redirect()로 유출됩니다. 오픈 리디렉션 피싱 방지를 위해 사전 검출 검사하세요. | `python/security/open-redirect.yaml` |
| `os-system-injection` | `ERROR` | os.system() 가승 연산 버퍼에 동적 조합이 도칩니다. Command Injection 피싱 격량을 타개하기 위해 철회하십시오. | `python/security/injection/os-system-injection.yaml` |
| `path-traversal-open` | `ERROR` | open() 가동에 유저 노출 경로 데이터가 전사됩니다. 로컬 파일 시스템 탈취를 막고자 사전에 정적 이격 시키세요. | `python/security/injection/path-traversal-open.yaml` |
| `raw-html-format` | `WARNING` | raw-html-format 잠재적 결함 분기 정비 요망. | `python/security/injection/raw-html-concat.yaml` |
| `ssrf-requests` | `ERROR` | HTTP 발송 인자에 오염 입력이 통과 탑재됩니다. SSRF 탈취를 예방하기 위해 조율 주소를 엄밀하게 교정하세요. | `python/security/injection/ssrf-requests.yaml` |
| `subprocess-injection` | `ERROR` | subprocess 가동 인자에 가변 오염 입력이 포착되었습니다. 커맨드 인젝션 격퇴를 위해 파라미터 분할 배열형을 고수하십시오. | `python/security/injection/subprocess-injection.yaml` |
| `tainted-sql-string` | `ERROR` | 동적 SQL 문자열 결합이 포착되었습니다. SQL Injection 배후 차단을 위해 Parameterized Query를 사수하세요. | `python/security/injection/tainted-sql-string.yaml` |
| `use-jsonify` | `ERROR` | 수동 딕셔너리 연계 반환보다 flask.jsonify() 내장 헬퍼를 이용하여 안정성을 유치할 것을 권합합니다. | `python/best-practice/use-jsonify.yaml` |
## JINJA2

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/autoescape-disabled-false.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/missing-autoescape-disabled.yaml` |
## JWT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `python/security/audit/jwt-exposed-data.yaml` |
| `jwt-python-exposed-credentials` | `ERROR` | JWT 토큰 페이로드에 기밀 정보가 노출되었습니다. 페이로드는 암호화되지 않으므로 유출 위험이 큽니다. | `python/security/jwt-exposed-credentials.yaml` |
| `jwt-python-hardcoded-secret` | `ERROR` | 하드코딩된 JWT 비밀키가 발견되었습니다. 보안 누출 예방을 위해 환경 변수나 보안 저장소로 이전하세요. | `python/security/jwt-hardcode.yaml` |
| `jwt-python-none-alg` | `ERROR` | JWT 서명 검증에서 none 알고리즘 사용이 감지되었습니다. 변조 토큰 우회를 피하기 위해 명시적 암호화를 지정하세요. | `python/security/jwt-none-alg.yaml` |
| `unverified-jwt-decode` | `ERROR` | JWT 디코딩 시 verify=False 플래그가 발견되었습니다. 무결성 검증을 위해 누출 옵션을 활성화하십시오. | `python/security/unverified-jwt-decode.yaml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/use-defusedcsv.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithms.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/dangerous-globals-use.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/dangerous-subinterpreters-run-string.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/dangerous-testcapi-run-in-subinterp.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/dangerous-code-run.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithms-md5.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-function.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/telnetlib.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/exec-detected.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/non-literal-import.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/audit/dangerous-subinterpreters-run-string-tainted-env-args.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/marshal.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/audit/dangerous-subinterpreters-run-string-audit.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/hardcoded-password-default-argument.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/audit/dangerous-code-run-tainted-env-args.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/python-reverse-shell.yaml` |
| `-` | `INFO` | 위험한 메서드 호출 피로도 점검. | `python/security/audit/dangerous-annotations-usage.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/audit/dangerous-testcapi-run-in-subinterp-tainted-env-args.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/formatted-sql-query.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/weak-ssl-version.yaml` |
| `-` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/mako-templates-detected.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/httpsconnection-detected.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/audit/dangerous-testcapi-run-in-subinterp-audit.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/ssl-wrap-socket-is-deprecated.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/paramiko-implicit-trust-host-key.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/regex-dos.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/sha224-hash.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/eval-detected.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/conn_recv.yaml` |
| `-` | `WARNING` | 위험한 메서드 호출 피로도 점검. | `python/security/audit/dangerous-code-run-audit.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/dynamic-urllib-use-detected.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/system-wildcard-detected.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/audit/sqli/asyncpg-sqli.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/audit/sqli/pg8000-sqli.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/audit/sqli/psycopg-sqli.yaml` |
| `-` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `python/security/audit/sqli/aiopg-sqli.yaml` |
| `-` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/network/bind.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/logging/logger-credential-leak.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/logging/listeneval.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/insecure-transport/ssl/no-set-ciphers.yaml` |
| `-` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/insecure-transport/ftplib/use-ftp-tls.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-request-object-ftp.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-openerdirector-open-ftp.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-request-object.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlopen-ftp.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlopener-retrieve.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-openerdirector-open.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlopener-open.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlopener-open-ftp.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlretrieve.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlopener-retrieve-ftp.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlretrieve-ftp.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-transport/urllib/insecure-urlopen.yaml` |
| `-` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/insecure-transport/requests/request-session-http-in-with-context.yaml` |
| `-` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/insecure-transport/requests/request-with-http.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/deserialization/avoid-jsonpickle.yaml` |
| `-` | `WARNING` | avoid-shelve 잠재적 결함 분기 정비 요망. | `python/security/deserialization/pickle.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/cannot-cache-generators.yaml` |
| `arbitrary-sleep` | `ERROR` | 가동 루프 내 sleep 병목 지점을 확인하세요. | `python/best-practice/sleep.yaml` |
| `avoid-pyyaml-load` | `ERROR` | 취약한 PyYAML load() 가동이 식별되었습니다. RCE 예방을 위해 safe_load()로 대두 전개 처리 하십시오. | `python/security/deserialization/avoid-pyyaml-load.yaml` |
| `avoid-unsafe-ruamel` | `ERROR` | 동격의 Yaml 파서 인 안전하지 않은 로드 형태 포착. safe 로드 구성을 승인하세요. | `python/security/deserialization/avoid-unsafe-ruamel.yaml` |
| `baseclass-attribute-override` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/baseclass-attribute-override.yaml` |
| `dangerous-asyncio-create-exec-audit` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-asyncio-create-exec-audit 전사 단속이 요구됩니다. | `python/security/audit/dangerous-asyncio-create-exec-audit.yaml` |
| `dangerous-asyncio-create-exec-tainted-env-args` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-asyncio-create-exec-tainted-env-args 전사 단속이 요구됩니다. | `python/security/audit/dangerous-asyncio-create-exec-tainted-env-args.yaml` |
| `dangerous-asyncio-exec-audit` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-asyncio-exec-audit 전사 단속이 요구됩니다. | `python/security/audit/dangerous-asyncio-exec-audit.yaml` |
| `dangerous-asyncio-exec-tainted-env-args` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-asyncio-exec-tainted-env-args 전사 단속이 요구됩니다. | `python/security/audit/dangerous-asyncio-exec-tainted-env-args.yaml` |
| `dangerous-asyncio-shell-audit` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-asyncio-shell-audit 전사 단속이 요구됩니다. | `python/security/audit/dangerous-asyncio-shell-audit.yaml` |
| `dangerous-asyncio-shell-tainted-env-args` | `ERROR` | 환경 변수 또는 외부 입력이 비동기 쉘 명령 실행에 직접 사용되고 있습니다. 커맨드 인젝션 취약점을 예방하려면 입력값을 엄격히 검증하거나 쉘을 사용하지 않는 방식으로 전환하세요. | `python/security/audit/dangerous-asyncio-shell-tainted-env-args.yaml` |
| `dangerous-os-exec` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-os-exec 전사 단속이 요구됩니다. | `python/security/dangerous-os-exec.yaml` |
| `dangerous-os-exec-audit` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-os-exec-audit 전사 단속이 요구됩니다. | `python/security/audit/dangerous-os-exec-audit.yaml` |
| `dangerous-os-exec-tainted-env-args` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-os-exec-tainted-env-args 전사 단속이 요구됩니다. | `python/security/audit/dangerous-os-exec-tainted-env-args.yaml` |
| `dangerous-spawn-process` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-spawn-process 전사 단속이 요구됩니다. | `python/security/dangerous-spawn-process.yaml` |
| `dangerous-spawn-process-audit` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-spawn-process-audit 전사 단속이 요구됩니다. | `python/security/audit/dangerous-spawn-process-audit.yaml` |
| `dangerous-spawn-process-tainted-env-args` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-spawn-process-tainted-env-args 전사 단속이 요구됩니다. | `python/security/audit/dangerous-spawn-process-tainted-env-args.yaml` |
| `dangerous-subprocess-use` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-subprocess-use 전사 단속이 요구됩니다. | `python/security/dangerous-subprocess-use.yaml` |
| `dangerous-subprocess-use-audit` | `ERROR` | 하위 프로세스 생성 드라이버 인젝션 전담 점검. | `python/security/audit/dangerous-subprocess-use-audit.yaml` |
| `dangerous-subprocess-use-tainted-env-args` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-subprocess-use-tainted-env-args 전사 단속이 요구됩니다. | `python/security/audit/dangerous-subprocess-use-tainted-env-args.yaml` |
| `dangerous-system-call` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-system-call 전사 단속이 요구됩니다. | `python/security/dangerous-system-call.yaml` |
| `dangerous-system-call-audit` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-system-call-audit 전사 단속이 요구됩니다. | `python/security/audit/dangerous-system-call-audit.yaml` |
| `dangerous-system-call-tainted-env-args` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 dangerous-system-call-tainted-env-args 전사 단속이 요구됩니다. | `python/security/audit/dangerous-system-call-tainted-env-args.yaml` |
| `default-mutable-dict` | `ERROR` | 가변 Dict를 함수 기본 인자로 탑재하여 전역 버퍼 오염을 유도하는 Pitfall을 경보합니다. None 분기가 요망됩니다. | `python/correctness/common-mistakes/default-mutable-dict.yaml` |
| `default-mutable-list` | `ERROR` | 가변 List 기본 인자 탑재로 인한 버퍼 공유 격량. None 매핑으로 필터 하세요. | `python/correctness/common-mistakes/default-mutable-list.yaml` |
| `dict-del-while-iterate` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/dict-modify-iterating.yaml` |
| `disabled-cert-validation` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 disabled-cert-validation 전사 단속이 요구됩니다. | `python/security/audit/network/disabled-cert-validation.yaml` |
| `file-object-redefined-before-close` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/file-object-redefined-before-close.yaml` |
| `hardcoded-tmp-path` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/hardcoded-tmp-path.yaml` |
| `http-not-https-connection` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 http-not-https-connection 전사 단속이 요구됩니다. | `python/security/audit/network/http-not-https-connection.yaml` |
| `improper-list-concat` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/maintainability/improper-list-concat.yaml` |
| `insecure-file-permissions` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/audit/insecure-file-permissions.yaml` |
| `insecure-uuid-version` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-uuid-version.yaml` |
| `is-function-without-parentheses` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/maintainability/is-function-without-parentheses.yaml` |
| `is-not-is-not` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 is-not-is-not 전사 단속이 요구됩니다. | `python/correctness/common-mistakes/is-not-is-not.yaml` |
| `list-modify-while-iterate` | `ERROR` | for 루프 순회 중 원본 리스트를 가변 삭제/수정하여 오동작을 격발시키는 리스크 누출입니다. | `python/correctness/list-modify-iterating.yaml` |
| `logging-error-without-handling` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/logging-error-without-handling.yaml` |
| `manual-defaultdict-dict-create` | `WARNING` | manual-counter-create 잠재적 결함 분기 정비 요망. | `python/best-practice/manual-collections-create.yaml` |
| `md5-used-as-password` | `WARNING` | md5-used-as-password 잠재적 결함 분기 정비 요망. | `python/security/audit/md5-used-as-password.yaml` |
| `missing-hash-with-eq` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/missing-hash-with-eq.yaml` |
| `no-strings-as-booleans` | `ERROR` | 문자열 결합 시 연산 논리 혼선 국면을 저지합니다. | `python/correctness/useless-comparison.yaml` |
| `open-never-closed` | `ERROR` | 디스크 서술자 닫기 연쇄가 빠졌습니다. | `python/best-practice/open-never-closed.yaml` |
| `paramiko-exec-command` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 paramiko-exec-command 전사 단속이 요구됩니다. | `python/security/audit/paramiko/paramiko-exec-command.yaml` |
| `pass-body-fn` | `WARNING` | pass-body-range 잠재적 결함 분기 정비 요망. | `python/best-practice/pass-body.yaml` |
| `pdb-remove` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/pdb.yaml` |
| `pytest-assert_match-after-path-patch` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/pytest-assert_match-after-path-patch.yaml` |
| `python-debugger-found` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/pdb.yaml` |
| `python36-compatibility-ssl` | `ERROR` | 해당 함수 및 문법은 해당 파이썬 버전 연산 지원 사양입니다. | `python/compatibility/python36.yaml` |
| `python37-compatibility-importlib` | `ERROR` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/compatibility/python37.yaml` |
| `raise-not-base-exception` | `ERROR` | raise-not-base-exception 보안 인젝션 및 런타임 버그 전사 예방 점검. | `python/correctness/exceptions/exceptions.yaml` |
| `request-session-with-http` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/audit/insecure-transport/requests/request-session-with-http.yaml` |
| `code-after-unconditional-return` | `WARNING` | return-not-in-function 잠재적 결함 분기 정비 요망. | `python/maintainability/return.yaml` |
| `string-concat-in-list` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/common-mistakes/string-concat-in-list.yaml` |
| `identical-is-comparison` | `ERROR` | 문자열 동등 비교는 is가 아닌 == 연산자를 구동하여 안정성을 가두어야 합니다. | `python/correctness/common-mistakes/is-comparison-string.yaml` |
| `subprocess-list-passed-as-string` | `WARNING` | 하위 프로세스 생성 드라이버 인젝션 피로도 점검. | `python/security/audit/subprocess-list-passed-as-string.yaml` |
| `subprocess-shell-true` | `ERROR` | shell=True 위험 옵션을 우회 처리하십시오. | `python/security/audit/subprocess-shell-true.yaml` |
| `sync-sleep-in-async-code` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/sync-sleep-in-async-code.yaml` |
| `tempfile-insecure` | `ERROR` | tempfile-insecure 보안 인젝션 및 런타임 버그 전사 예방 점검. | `python/correctness/tempfile/mktemp.yaml` |
| `tempfile-without-flush` | `ERROR` | tempfile-without-flush 보안 인젝션 및 런타임 버그 전사 예방 점검. | `python/correctness/tempfile/flush.yaml` |
| `test-is-missing-assert` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/test-is-missing-assert.yaml` |
| `uncaught-executor-exceptions` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/concurrent.yaml` |
| `unchecked-subprocess-call` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/unchecked-returns.yaml` |
| `unspecified-open-encoding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/unspecified-open-encoding.yaml` |
| `unverified-ssl-context` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 unverified-ssl-context 전사 단속이 요구됩니다. | `python/security/unverified-ssl-context.yaml` |
| `use-defused-xml` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 use-defused-xml 전사 단속이 요구됩니다. | `python/security/use-defused-xml.yaml` |
| `use-defused-xml-parse` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 use-defused-xml-parse 전사 단속이 요구됩니다. | `python/security/use-defused-xml-parse.yaml` |
| `use-defused-xmlrpc` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 use-defused-xmlrpc 전사 단속이 요구됩니다. | `python/security/use-defused-xmlrpc.yaml` |
| `use-sys-exit` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/exit.yaml` |
| `useless-assignment-keyed` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/maintainability/useless-assign-keyed.yaml` |
| `useless-eqeq` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/useless-eqeq.yaml` |
| `useless-if-conditional` | `WARNING` | useless-if-body 잠재적 결함 분기 정비 요망. | `python/maintainability/useless-ifelse.yaml` |
| `useless-inner-function` | `ERROR` | 내부 함수가 정의되었으나 사용 연동이 누락되었습니다. 코드 가독성을 위해 말소 대상 점검 하십시오. | `python/maintainability/useless-innerfunction.yaml` |
| `useless-literal` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/maintainability/useless-literal.yaml` |
| `useless-literal-set` | `ERROR` | set 조립 단에 중복 할당 조각이 보입니다. | `python/maintainability/useless-literal-set.yaml` |
| `writing-to-file-in-read-mode` | `ERROR` | 읽기 전용으로 가동 중인 서술자에 쓰기(.write)를 가압하는 런타임 에러를 경보합니다. | `python/correctness/writing-to-file-in-read-mode.yaml` |
| `return-in-init` | `ERROR` | __init__ 내부 yield 전사 금기 양식. | `python/correctness/return-in-init.yaml` |
## PYCRYPTODOME

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithm-md4.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/insufficient-rsa-key-size.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithm-md5.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithm-rc2.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithm-blowfish.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithm-md2.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithm.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithm-rc4.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/insufficient-dsa-key-size.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-hash-algorithm.yaml` |
| `-` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `python/security/insecure-cipher-algorithm-des.yaml` |
| `crypto-mode-without-authentication` | `ERROR` | 무결성 검증 없는 암호화 운영 모드가 사용되었습니다. GCM 등 AEAD 모드 사용을 권장합니다. | `python/security/mode-without-authentication.yaml` |
## PYMONGO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/mongodb.yaml` |
## PYRAMID

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/authtkt-cookie-secure-unsafe-default.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/authtkt-cookie-secure-unsafe-value.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/set-cookie-samesite-unsafe-value.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/set-cookie-httponly-unsafe-value.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/audit/csrf-check-disabled.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/authtkt-cookie-httponly-unsafe-value.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/authtkt-cookie-httponly-unsafe-default.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/set-cookie-secure-unsafe-default.yaml` |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/audit/csrf-origin-check-disabled.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/authtkt-cookie-samesite.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/set-cookie-secure-unsafe-value.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/set-cookie-httponly-unsafe-default.yaml` |
| `-` | `WARNING` | 쿠키 보안 속성(Secure/HttpOnly) 누출 우려 점검. | `python/audit/set-cookie-samesite-unsafe-default.yaml` |
| `pyramid-csrf-check-disabled-globally` | `ERROR` | Pyramid 전역 CSRF 방어가 비활성화되었습니다. 뷰 보호를 위해 토크나이저를 구동하십시오. | `python/security/csrf-check-disabled-globally.yaml` |
| `pyramid-csrf-origin-check-disabled-globally` | `ERROR` | Pyramid Referrer CSRF 검증 전역 누락 감지. Origin 체크 옵션을 활성화하십시오. | `python/audit/csrf-origin-check-disabled-globally.yaml` |
| `pyramid-direct-use-of-response` | `ERROR` | Response 객체를 렌더링 없이 날것으로 출력합니다. XSS 방어 레이어 우회 우려가 있습니다. | `python/security/direct-use-of-response.yaml` |
| `pyramid-sqlalchemy-sql-injection` | `ERROR` | distinct/having절 내 원시 SQL 인젝션 가동 현상 감지. bindparams 규격을 준행하세요. | `python/security/sqlalchemy-sql-injection.yaml` |
## REQUESTS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `disabled-cert-validation` | `ERROR` | requests 가동 시 인증서 검증(verify=False) 탑재는 중간자 노출을 가동합니다. 재활성화 하세요. | `python/security/disabled-cert-validation.yaml` |
| `no-auth-over-http` | `ERROR` | HTTP 평문 채널을 통한 인증 전송이 포착되었습니다. 스니핑 격퇴를 위해 https:// 도약을 강제 하십시오. | `python/security/no-auth-over-http.yaml` |
| `python.requests.best-practice.use-request-json-shortcut` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/use-request-json-shortcut.yaml` |
| `python.requests.best-practice.use-response-json-shortcut` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/use-response-json-shortcut.yaml` |
| `use-raise-for-status` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/use-raise-for-status.yaml` |
| `use-timeout` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/best-practice/use-timeout.yaml` |
## SH

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `string-concat` | `ERROR` | 인젝션 및 논리 에러 예방을 위한 string-concat 전사 단속이 요구됩니다. | `python/security/string-concat.yaml` |
## SQLALCHEMY

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `-` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/security/sqlalchemy-sql-injection.yaml` |
| `avoid-sqlalchemy-text` | `ERROR` | sqlalchemy.text() 구동 시 동적 스트링 합사 버그가 우려됩니다. ORM 규격 모델을 준행 하세요. | `python/security/audit/avoid-sqlalchemy-text.yaml` |
| `bad-operator-in-filter` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `python/correctness/bad-operator-in-filter.yaml` |
| `len-all-count` | `WARNING` | batch-import 잠재적 결함 분기 정비 요망. | `python/performance/performance-improvements.yaml` |
| `delete-where-no-execute` | `ERROR` | SQLAlchemy .delete() 연산 시 .where() 스킵 국면이 감지되었습니다. 전사 데이터 유실에 대비하십시오. | `python/correctness/delete-where.yaml` |
| `sqlalchemy-execute-raw-query` | `ERROR` | SQLAlchemy 원시 쿼리 동적 결합 감지. 인젝션 투하를 막으려면 Parameterized 선언을 장착 하십시오. | `python/security/sqlalchemy-execute-raw-query.yaml` |
## TWILIO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `twiml-injection` | `WARNING` | twiml-injection 잠재적 결함 분기 정비 요망. | `python/security/twiml-injection.yaml` |
# Language: STATS

## ROOT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `cwe_to_metacategory` | `INFO` | 규칙에 대한 상세 설명이 제공되지 않았습니다. | `stats/cwe_to_metacategory.yml` |
| `metacategory_to_support_tier` | `INFO` | 규칙에 대한 상세 설명이 제공되지 않았습니다. | `stats/metacategory_to_support_tier.yml` |
| `web_frameworks` | `INFO` | 규칙에 대한 상세 설명이 제공되지 않았습니다. | `stats/web_frameworks.yml` |
# Language: SWIFT

## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: insecure-random` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `swift/crypto/insecure-random.yaml` |
| `- id: swift-user-defaults` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `swift/storage/sensitive-storage-userdefaults.yaml` |
## SQLLITE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: swift-potential-sqlite-injection` | `WARNING` | 잠재적 SQL 인젝션 경로에 대한 검토가 필요합니다. | `swift/sqllite-injection-audit.yaml` |
## WEBVIEW

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: swift-webview-config-allows-js-open-windows` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `swift/webview-js-window.yaml` |
# Language: TERRAFORM

## AWS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: aws-athena-client-can-disable-workgroup-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-athena-client-can-disable-workgroup-encryption.yaml` |
| `- id: aws-athena-database-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-athena-database-unencrypted.yaml` |
| `- id: aws-athena-workgroup-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-athena-workgroup-unencrypted.yaml` |
| `- id: aws-backup-vault-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-backup-vault-unencrypted.yaml` |
| `- id: aws-cloudtrail-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-cloudtrail-encrypted-with-cmk.yaml` |
| `- id: aws-cloudwatch-log-group-no-retention` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-cloudwatch-log-group-no-retention.yaml` |
| `- id: aws-cloudwatch-log-group-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-cloudwatch-log-group-unencrypted.yaml` |
| `- id: aws-codebuild-artifacts-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-codebuild-artifacts-unencrypted.yaml` |
| `- id: aws-codebuild-project-artifacts-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-codebuild-project-artifacts-unencrypted.yaml` |
| `- id: aws-codebuild-project-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-codebuild-project-unencrypted.yaml` |
| `- id: aws-config-aggregator-not-all-regions` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-config-aggregator-not-all-regions.yaml` |
| `- id: aws-db-instance-no-logging` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-db-instance-no-logging.yaml` |
| `- id: aws-docdb-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-docdb-encrypted-with-cmk.yaml` |
| `- id: aws-documentdb-auditing-disabled` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-documentdb-auditing-disabled.yaml` |
| `- id: aws-documentdb-storage-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-documentdb-storage-unencrypted.yaml` |
| `- id: aws-dynamodb-point-in-time-recovery-disabled` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-dynamodb-point-in-time-recovery-disabled.yaml` |
| `- id: aws-dynamodb-table-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-dynamodb-table-unencrypted.yaml` |
| `- id: aws-ebs-snapshot-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ebs-snapshot-encrypted-with-cmk.yaml` |
| `- id: aws-ebs-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ebs-unencrypted.yaml` |
| `- id: aws-ebs-volume-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ebs-volume-encrypted-with-cmk.yaml` |
| `- id: aws-ebs-volume-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ebs-volume-unencrypted.yaml` |
| `- id: aws-ec2-has-public-ip` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ec2-has-public-ip.yaml` |
| `- id: aws-ec2-launch-configuration-ebs-block-device-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ec2-launch-configuration-ebs-block-device-unencrypted.yaml` |
| `- id: aws-ec2-launch-configuration-root-block-device-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ec2-launch-configuration-root-block-device-unencrypted.yml` |
| `- id: aws-ec2-launch-template-metadata-service-v1-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ec2-launch-template-metadata-service-v1-enabled.yaml` |
| `- id: aws-ec2-security-group-allows-public-ingress` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ec2-security-group-allows-public-ingress.yaml` |
| `aws-ec2-security-group-rule-missing-description` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ec2-security-group-rule-missing-description.yaml` |
| `- id: aws-ecr-image-scanning-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ecr-image-scanning-disabled.yaml` |
| `- id: aws-ecr-mutable-image-tags` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ecr-mutable-image-tags.yaml` |
| `- id: aws-ecr-repository-wildcard-principal` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ecr-repository-wildcard-principal.yaml` |
| `- id: aws-efs-filesystem-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-efs-filesystem-encrypted-with-cmk.yaml` |
| `- id: aws-elasticache-automatic-backup-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/aws-elasticache-automatic-backup-not-enabled.yaml` |
| `- id: aws-elasticsearch-insecure-tls-version` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/aws-elasticsearch-insecure-tls-version.yaml` |
| `- id: aws-elasticsearch-nodetonode-encryption-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-elasticsearch-nodetonode-encryption.yaml` |
| `- id: aws-elb-access-logs-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-elb-access-logs-not-enabled.yaml` |
| `- id: aws-emr-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-emr-encrypted-with-cmk.yaml` |
| `- id: aws-fsx-lustre-filesystem-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-fsx-lustre-files-ystem.yaml` |
| `- id: aws-fsx-lustre-filesystem-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-fsx-lustre-filesystem-encrypted-with-cmk.yaml` |
| `- id: aws-fsx-ontapfs-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-fsx-ontapfs-encrypted-with-cmk.yaml` |
| `- id: aws-fsx-windows-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-fsx-windows-encrypted-with-cmk.yaml` |
| `aws-glacier-vault-any-principal` | `ERROR` | IaC 보안 옵션 누락 경보: aws-glacier-vault-any-principal 설정율을 적용해 주십시오. | `terraform/security/aws-glacier-vault-any-principal.yaml` |
| `aws-iam-admin-policy` | `ERROR` | IaC 보안 옵션 누락 경보: aws-iam-admin-policy 설정율을 적용해 주십시오. | `terraform/security/aws-iam-admin-policy.yaml` |
| `aws-iam-admin-policy-ssoadmin` | `ERROR` | IaC 보안 옵션 누락 경보: aws-iam-admin-policy-ssoadmin 설정율을 적용해 주십시오. | `terraform/security/aws-iam-admin-policy-ssoadmin.yaml` |
| `- id: aws-imagebuilder-component-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-imagebuilder-component-encrypted-with-cmk.yaml` |
| `- id: aws-insecure-api-gateway-tls-version` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/aws-insecure-api-gateway-tls-version.yaml` |
| `- id: aws-insecure-cloudfront-distribution-tls-version` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/aws-cloudfront-insecure-tls.yaml` |
| `- id: aws-insecure-redshift-ssl-configuration` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/aws-insecure-redshift-ssl-configuration.yaml` |
| `- id: aws-kinesis-stream-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-kinesis-stream-encrypted-with-cmk.yaml` |
| `- id: aws-kinesis-stream-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-kinesis-stream-unencrypted.yaml` |
| `- id: aws-kinesis-video-stream-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-kinesis-video-stream-encrypted-with-cmk.yaml` |
| `aws-kms-key-wildcard-principal` | `ERROR` | Terraform 구성 중 와일드카드(*) 권한 과잉 부여가 식별되었습니다. | `terraform/security/aws-kms-key-wildcard-principal.yaml` |
| `- id: aws-kms-no-rotation` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-kms-no-rotation.yaml` |
| `aws-lambda-environment-credentials` | `ERROR` | IaC 보안 옵션 누락 경보: aws-lambda-environment-credentials 설정율을 적용해 주십시오. | `terraform/security/aws-lambda-environment-credentials.yaml` |
| `- id: aws-lambda-environment-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-lambda-environment-unencrypted.yaml` |
| `aws-lambda-permission-unrestricted-source-arn` | `ERROR` | IaC 보안 옵션 누락 경보: aws-lambda-permission-unrestricted-source-arn 설정율을 적용해 주십시오. | `terraform/security/aws-lambda-permission-unrestricted-source-arn.yaml` |
| `- id: aws-lambda-x-ray-tracing-not-active` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-lambda-x-ray-tracing-not-active.yaml` |
| `- id: aws-network-acl-allows-all-ports` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-network-acl-allows-all-ports.yaml` |
| `- id: aws-network-acl-allows-public-ingress` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-network-acl-allows-public-ingress.yaml` |
| `aws-opensearchserverless-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-opensearchserverless-encrypted-with-cmk.yaml` |
| `- id: aws-provider-static-credentials` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-provider-static-credentials.yaml` |
| `aws-provisioner-exec` | `WARNING` | 프로비저너는 최후의 수단이므로 가능하면 피해야 합니다. 프로비저너 동작은 Terraform에서 계획의 일부로 매핑할 수 없으며 설계에 따라 임의의 셸 명령을 실행합니다. | `terraform/security/aws-provisioner-exec.yaml` |
| `- id: aws-qldb-inadequate-ledger-permissions-mode` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/aws-qldb-inadequate-ledger-permissions-mode.yaml` |
| `- id: aws-rds-backup-no-retention` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-rds-backup-no-retention.yaml` |
| `- id: aws-rds-cluster-iam-authentication-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/aws-rds-cluster-iam-authentication-not-enabled.yaml` |
| `- id: aws-rds-iam-authentication-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/aws-rds-iam-authentication-not-enabled.yaml` |
| `- id: aws-rds-multiaz-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/aws-rds-multiaz-not-enabled.yaml` |
| `- id: aws-redshift-cluster-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-redshift-cluster-encrypted-with-cmk.yaml` |
| `- id: aws-s3-bucket-object-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-s3-bucket-object-encrypted-with-cmk.yaml` |
| `- id: aws-s3-bucket-versioning-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/aws-s3-bucket-versioning-not-enabled.yaml` |
| `- id: aws-s3-object-copy-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-s3-object-copy-encrypted-with-cmk.yaml` |
| `- id: aws-s3-object-lock-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/aws-s3-object-lock-not-enabled.yaml` |
| `- id: aws-sagemaker-domain-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-sagemaker-domain-encrypted-with-cmk.yaml` |
| `- id: aws-secretsmanager-secret-unencrypted` | `WARNING` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `terraform/security/aws-secretsmanager-secret-unencrypted.yaml` |
| `- id: aws-sns-topic-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-sns-topic-unencrypted.yaml` |
| `- id: aws-sqs-queue-policy-wildcard-action` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-sqs-queue-policy-wildcard-action.yaml` |
| `aws-sqs-queue-policy-wildcard-principal` | `ERROR` | Terraform 구성 중 와일드카드(*) 권한 과잉 부여가 식별되었습니다. | `terraform/security/aws-sqs-queue-policy-wildcard-principal.yaml` |
| `- id: aws-sqs-queue-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-sqs-queue-unencrypted.yaml` |
| `- id: aws-ssm-document-logging-issues` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-ssm-document-logging-issues.yaml` |
| `- id: aws-subnet-has-public-ip-address` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-subnet-has-public-ip-address.yaml` |
| `- id: aws-timestream-database-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-timestream-database-encrypted-with-cmk.yaml` |
| `- id: aws-transfer-server-is-public` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-transfer-server-is-public.yaml` |
| `- id: aws-workspaces-root-volume-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-workspaces-root-volume-unencrypted.yaml` |
| `- id: aws-workspaces-user-volume-unencrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aws-workspaces-user-volume-unencrypted.yaml` |
| `- id: insecure-load-balancer-tls-version` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/insecure-load-balancer-tls-version.yaml` |
| `lambda-permission-logs-missing-arn-asterisk` | `WARNING` | lambda-permission-logs-missing-arn-asterisk IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/correctness/lambda-permission-logs-missing-arn-asterisk.yaml` |
| `lambda-redundant-field-with-image` | `WARNING` | lambda-redundant-field-with-image IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/correctness/lambda-redundant-field-with-image.yaml` |
| `missing-alb-drop-http-headers` | `WARNING` | missing-alb-drop-http-headers IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/best-practice/missing-alb-drop-http-headers.yaml` |
| `missing-api-gateway-cache-cluster` | `WARNING` | missing-api-gateway-cache-cluster IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/best-practice/missing-api-gateway-cache-cluster.yaml` |
| `- id: missing-athena-workgroup-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/missing-athena-workgroup-encryption.yaml` |
| `missing-autoscaling-group-tags` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/missing-autoscaling-group-tags.yaml` |
| `- id: missing-aws-autoscaling-tags` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/missing-aws-autoscaling-tags.yaml` |
| `- id: missing-aws-cross-zone-lb` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/missing-aws-cross-zone-lb.yaml` |
| `- id: missing-aws-lb-deletion-protection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/missing-aws-lb-deletion-protection.yaml` |
| `- id: missing-aws-qldb-deletion-protection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/missing-aws-qldb-deletion-protection.yaml` |
| `missing-cloudwatch-log-group-kms-key` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/missing-cloudwatch-log-group-kms-key.yaml` |
| `missing-cloudwatch-log-group-retention` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/missing-cloudwatch-log-group-retention.yaml` |
| `- id: reserved-aws-lambda-environment-variable` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/correctness/reserved-aws-lambda-environment-variable.yaml` |
| `subscription-filter-missing-depends` | `WARNING` | subscription-filter-missing-depends IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/correctness/subscription-filter-missing-depends.yaml` |
| `unrestricted-github-oidc-policy` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/unrestricted-github-oidc-policy.yaml` |
| `wildcard-assume-role` | `ERROR` | Terraform 구성 중 와일드카드(*) 권한 과잉 부여가 식별되었습니다. | `terraform/security/wildcard-assume-role.yaml` |
## AZURE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: appservice-account-identity-registered` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/appservice-account-identity-registered.yaml` |
| `appservice-authentication-enabled` | `ERROR` | IaC 보안 옵션 누락 경보: appservice-authentication-enabled 설정율을 적용해 주십시오. | `terraform/security/appservice/appservice-authentication-enabled.yaml` |
| `- id: appservice-enable-http2` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/appservice-enable-http2.yaml` |
| `appservice-enable-https-only` | `ERROR` | IaC 보안 옵션 누락 경보: appservice-enable-https-only 설정율을 적용해 주십시오. | `terraform/security/appservice/appservice-enable-https-only.yaml` |
| `- id: appservice-require-client-cert` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/appservice-require-client-cert.yaml` |
| `appservice-use-secure-tls-policy` | `ERROR` | IaC 보안 옵션 누락 경보: appservice-use-secure-tls-policy 설정율을 적용해 주십시오. | `terraform/security/appservice/appservice-use-secure-tls-policy.yaml` |
| `azure-ad-used-auth-service-fabric` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-ad-used-auth-service-fabric.yaml` |
| `- id: azure-aks-apiserver-auth-ip-ranges` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aks/azure-aks-apiserver-auth-ip-ranges.yaml` |
| `- id: azure-aks-private-clusters-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aks/azure-aks-private-clusters-enabled.yaml` |
| `azure-aks-uses-azure-policies-addon` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-aks-uses-azure-policies-addon.yaml` |
| `- id: azure-aks-uses-disk-encryptionset` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/aks/azure-aks-uses-disk-encryptionset.yaml` |
| `- id: azure-apiservices-use-virtualnetwork` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/apiservice/azure-apiservices-use-virtualnetwork.yaml` |
| `azure-appgateway-enables-waf` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appgateway-enables-waf.yaml` |
| `- id: azure-appservice-auth` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-auth.yaml` |
| `- id: azure-appservice-client-certificate` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-client-certificate.yaml` |
| `- id: azure-appservice-detailed-errormessages-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-detailed-errormessages-enabled.yaml` |
| `- id: azure-appservice-disallowed-cors` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-disallowed-cors.yaml` |
| `azure-appservice-dotnet-framework-version` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appservice-dotnet-framework-version.yaml` |
| `- id: azure-appservice-enabled-failed-request` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-enabled-failed-request.yaml` |
| `azure-appservice-ftps-state` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appservice-ftps-state.yaml` |
| `- id: azure-appservice-http-logging-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-http-logging-enabled.yaml` |
| `azure-appservice-https-20-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appservice-https-20-enabled.yaml` |
| `- id: azure-appservice-https-only` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-https-only.yaml` |
| `- id: azure-appservice-identity` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-identity.yaml` |
| `- id: azure-appservice-identityprovider-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-identityprovider-enabled.yaml` |
| `azure-appservice-java-version` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appservice-java-version.yaml` |
| `- id: azure-appservice-min-tls-version` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/appservice/azure-appservice-min-tls-version.yaml` |
| `azure-appservice-php-version` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appservice-php-version.yaml` |
| `azure-appservice-python-version` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appservice-python-version.yaml` |
| `azure-appservice-used-azure-files` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-appservice-used-azure-files.yaml` |
| `- id: azure-automation-encrypted` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-automation-encrypted.yaml` |
| `- id: azure-batchaccount-uses-keyvault-encrpytion` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-batchaccount-uses-keyvault-encrpytion.yaml` |
| `- id: azure-cognitiveservices-disables-public-network` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-cognitiveservices-disables-public-network.yaml` |
| `- id: azure-containergroup-deployed-into-virtualnetwork` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-containergroup-deployed-into-virtualnetwork.yaml` |
| `- id: azure-cosmosdb-accounts-restricted-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-cosmosdb-accounts-restricted-access.yaml` |
| `- id: azure-cosmosdb-disable-access-key-write` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-cosmosdb-disable-access-key-write.yaml` |
| `- id: azure-cosmosdb-disables-public-network` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-cosmosdb-disables-public-network.yaml` |
| `- id: azure-cosmosdb-have-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-cosmosdb-have-cmk.yaml` |
| `- id: azure-customrole-definition-subscription-owner` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-customrole-definition-subscription-owner.yaml` |
| `- id: azure-dataexplorer-double-encryption-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-dataexplorer-double-encryption-enabled.yaml` |
| `- id: azure-dataexplorer-uses-disk-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-dataexplorer-uses-disk-encryption.yaml` |
| `- id: azure-datafactory-no-public-network-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-datafactory-no-public-network-access.yaml` |
| `- id: azure-datafactory-uses-git-repository` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-datafactory-uses-git-repository.yaml` |
| `- id: azure-datalake-store-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-datalake-store-encryption.yaml` |
| `azure-defenderon-appservices` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-appservices.yaml` |
| `azure-defenderon-container-registry` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-container-registry.yaml` |
| `azure-defenderon-keyvaults` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-keyvaults.yaml` |
| `azure-defenderon-kubernetes` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-kubernetes.yaml` |
| `azure-defenderon-servers` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-servers.yaml` |
| `azure-defenderon-sqlservers` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-sqlservers.yaml` |
| `azure-defenderon-sqlservers-vms` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-sqlservers-vms.yaml` |
| `azure-defenderon-storage` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-defenderon-storage.yaml` |
| `- id: azure-eventgrid-domain-network-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-eventgrid-domain-network-access.yaml` |
| `azure-frontdoor-enables-waf` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-frontdoor-enables-waf.yaml` |
| `azure-frontdoor-use-wafmode` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-frontdoor-use-wafmode.yaml` |
| `- id: azure-functionapp-disallow-cors` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-functionapp-disallow-cors.yaml` |
| `azure-functionapp-http-version-latest` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-functionapp-http-version-latest.yaml` |
| `azure-functionapps-accessible-over-https` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-functionapps-accessible-over-https.yaml` |
| `- id: azure-functionapps-enable-auth` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-functionapps-enable-auth.yaml` |
| `- id: azure-instance-extensions` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-instance-extensions.yaml` |
| `- id: azure-iot-no-public-network-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-iot-no-public-network-access.yaml` |
| `- id: azure-key-backedby-hsm` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-key-backedby-hsm.yaml` |
| `- id: azure-key-no-expiration-date` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-key-no-expiration-date.yaml` |
| `azure-keyvault-enables-firewall-rules-settings` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-keyvault-enables-firewall-rules-settings.yaml` |
| `azure-keyvault-enables-purge-protection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-keyvault-enables-purge-protection.yaml` |
| `azure-keyvault-enables-soft-delete` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-keyvault-enables-soft-delete.yaml` |
| `azure-keyvault-recovery-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-keyvault-recovery-enabled.yaml` |
| `- id: azure-managed-disk-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-managed-disk-encryption.yaml` |
| `- id: azure-managed-disk-encryption-set` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-managed-disk-encryption-set.yaml` |
| `azure-mariadb-geo-backup-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-mariadb-geo-backup-enabled.yaml` |
| `- id: azure-mariadb-public-access-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-mariadb-public-access-disabled.yaml` |
| `azure-mariadb-sslenforcement-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-mariadb-sslenforcement-enabled.yaml` |
| `azure-monitor-log-profile-categories` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-monitor-log-profile-categories.yaml` |
| `- id: azure-monitor-log-profile-retention-days` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-monitor-log-profile-retention-days.yaml` |
| `azure-monitor-log-profile-retention-days` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-monitor-log-profile-retention-days.yaml` |
| `- id: azure-mssql-service-mintls-version` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-mssql-service-mintls-version.yaml` |
| `- id: azure-mysql-encryption-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-mysql-encryption-enabled.yaml` |
| `azure-mysql-geo-backup-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-mysql-geo-backup-enabled.yaml` |
| `- id: azure-mysql-mintls-version` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-mysql-mintls-version.yaml` |
| `- id: azure-mysql-public-access-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-mysql-public-access-disabled.yaml` |
| `azure-mysql-server-tlsenforcement-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-mysql-server-tlsenforcement-enabled.yaml` |
| `azure-mysql-threat-detection-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-mysql-threat-detection-enabled.yaml` |
| `azure-network-watcher-flowlog-period` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-network-watcher-flowlog-period.yaml` |
| `- id: azure-networkinterface-enable-ip-forwarding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-networkinterface-enable-ip-forwarding.yaml` |
| `- id: azure-postgresql-encryption-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-postgresql-encryption-enabled.yaml` |
| `azure-postgresql-flexi-server-geo-backup-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-postgresql-flexi-server-geo-backup-enabled.yaml` |
| `azure-postgresql-geo-backup-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-postgresql-geo-backup-enabled.yaml` |
| `- id: azure-postgresql-min-tls-version` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-postgresql-min-tls-version.yaml` |
| `azure-postgresql-server-connection-throttling-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-postgresql-server-connection-throttling-enabled.yaml` |
| `azure-postgresql-server-log-checkpoint-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-postgresql-server-log-checkpoint-enabled.yaml` |
| `azure-postgresql-server-log-connections-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-postgresql-server-log-connections-enabled.yaml` |
| `- id: azure-postgresql-server-public-access-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-postgresql-server-public-access-disabled.yaml` |
| `azure-postgresql-ssl-enforcement-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-postgresql-ssl-enforcement-enabled.yaml` |
| `azure-postgresql-threat-detection-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-postgresql-threat-detection-enabled.yaml` |
| `- id: azure-redis-cache-enable-non-ssl-port` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-redis-cache-enable-non-ssl-port.yaml` |
| `- id: azure-redis-cache-public-network-access-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-redis-cache-public-network-access-enabled.yaml` |
| `- id: azure-remote-debugging-not-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-remote-debugging-not-enabled.yaml` |
| `- id: azure-scale-set-password` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-scale-set-password.yaml` |
| `- id: azure-search-publicnetwork-access-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-search-publicnetwork-access-disabled.yaml` |
| `azure-secret-content-type` | `WARNING` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `terraform/best-practice/azure-secret-content-type.yaml` |
| `azure-secret-expiration-date` | `WARNING` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `terraform/best-practice/azure-secret-expiration-date.yaml` |
| `azure-securitcenter-email-alert` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-securitcenter-email-alert.yaml` |
| `azure-securitycenter-contact-emails` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-securitycenter-contact-emails.yaml` |
| `azure-securitycenter-contact-phone` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-securitycenter-contact-phone.yaml` |
| `azure-securitycenter-email-alert-admins` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-securitycenter-email-alert-admins.yaml` |
| `azure-securitycenter-standard-pricing` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-securitycenter-standard-pricing.yaml` |
| `- id: azure-service-fabric-cluster-protection-level` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-service-fabric-cluster-protection-level.yaml` |
| `azure-sqlserver-email-alerts-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-sqlserver-email-alerts-enabled.yaml` |
| `azure-sqlserver-email-alerts-toadmins-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-sqlserver-email-alerts-toadmins-enabled.yaml` |
| `- id: azure-sqlserver-no-public-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-sqlserver-no-public-access.yaml` |
| `- id: azure-sqlserver-public-access-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-sqlserver-public-access-disabled.yaml` |
| `azure-sqlserver-threat-detection-types` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-sqlserver-threat-detection-types.yaml` |
| `- id: azure-storage-account-disable-public-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-storage-account-disable-public-access.yaml` |
| `azure-storage-account-enables-secure-transfer` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-storage-account-enables-secure-transfer.yaml` |
| `- id: azure-storage-account-minimum-tlsversion` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-storage-account-minimum-tlsversion.yaml` |
| `- id: azure-storage-blob-service-container-private-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-storage-blob-service-container-private-access.yaml` |
| `- id: azure-storage-sync-public-access-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-storage-sync-public-access-disabled.yaml` |
| `azure-synapse-workscape-enables-managed-virtual-network` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-synapse-workscape-enables-managed-virtual-network.yaml` |
| `- id: azure-vmencryption-at-host-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/azure-vmencryption-at-host-enabled.yaml` |
| `azure-vmscale-sets-auto-os-image-patching-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-vmscale-sets-auto-os-image-patching-enabled.yaml` |
| `azure-waf-specificed-mode-app-gw` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/azure-waf-specificed-mode-app-gw.yaml` |
| `- id: functionapp-authentication-enabled` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/functionapp/functionapp-authentication-enabled.yaml` |
| `- id: functionapp-enable-http2` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/functionapp/functionapp-enable-http2.yaml` |
| `- id: keyvault-content-type-for-secret` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `terraform/security/keyvault/keyvault-content-type-for-secret.yaml` |
| `- id: keyvault-ensure-key-expires` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/keyvault/keyvault-ensure-key-expires.yaml` |
| `- id: keyvault-ensure-secret-expires` | `INFO` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `terraform/security/keyvault/keyvault-ensure-secret-expires.yaml` |
| `- id: keyvault-purge-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/keyvault/keyvault-purge-enabled.yaml` |
| `keyvault-specify-network-acl` | `ERROR` | IaC 보안 옵션 누락 경보: keyvault-specify-network-acl 설정율을 적용해 주십시오. | `terraform/security/keyvault/keyvault-specify-network-acl.yaml` |
| `- id: storage-allow-microsoft-service-bypass` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/storage/storage-allow-microsoft-service-bypass.yaml` |
| `storage-default-action-deny` | `ERROR` | IaC 보안 옵션 누락 경보: storage-default-action-deny 설정율을 적용해 주십시오. | `terraform/security/storage/storage-default-action-deny.yaml` |
| `- id: storage-enforce-https` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/storage/storage-enforce-https.yaml` |
| `- id: storage-queue-services-logging` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/storage/storage-queue-services-logging.yaml` |
| `storage-use-secure-tls-policy` | `ERROR` | IaC 보안 옵션 누락 경보: storage-use-secure-tls-policy 설정율을 적용해 주십시오. | `terraform/security/storage/storage-use-secure-tls-policy.yaml` |
## GCP

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: gcp-artifact-registry-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-artifact-registry-encrypted-with-cmk.yaml` |
| `- id: gcp-artifact-registry-private-repo-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-artifact-registry-private-repo-iam-binding.yaml` |
| `- id: gcp-artifact-registry-private-repo-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-artifact-registry-private-repo-iam-member.yaml` |
| `- id: gcp-bigquery-dataset-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-bigquery-dataset-encrypted-with-cmk.yaml` |
| `- id: gcp-bigquery-private-table-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-bigquery-private-table-iam-binding.yaml` |
| `- id: gcp-bigquery-private-table-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-bigquery-private-table-iam-member.yaml` |
| `- id: gcp-bigquery-table-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-bigquery-table-encrypted-with-cmk.yaml` |
| `- id: gcp-bigtable-instance-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-bigtable-instance-encrypted-with-cmk.yaml` |
| `- id: gcp-build-workers-private` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-build-workers-private.yaml` |
| `- id: gcp-cloud-storage-logging` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-cloud-storage-logging.yaml` |
| `- id: gcp-compute-boot-disk-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-boot-disk-encryption.yaml` |
| `- id: gcp-compute-disk-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-disk-encryption.yaml` |
| `- id: gcp-compute-firewall-unrestricted-ingress-20` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-firewall-unrestricted-ingress-20.yaml` |
| `- id: gcp-compute-firewall-unrestricted-ingress-21` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-firewall-unrestricted-ingress-21.yaml` |
| `- id: gcp-compute-firewall-unrestricted-ingress-22` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-firewall-unrestricted-ingress-22.yaml` |
| `- id: gcp-compute-firewall-unrestricted-ingress-3306` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-firewall-unrestricted-ingress-3306.yaml` |
| `- id: gcp-compute-firewall-unrestricted-ingress-3389` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-firewall-unrestricted-ingress-3389.yaml` |
| `- id: gcp-compute-firewall-unrestricted-ingress-80` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-firewall-unrestricted-ingress-80.yaml` |
| `- id: gcp-compute-ip-forward` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-ip-forward.yaml` |
| `- id: gcp-compute-os-login` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-os-login.yaml` |
| `- id: gcp-compute-project-os-login` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-project-os-login.yaml` |
| `- id: gcp-compute-public-ip` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-public-ip.yaml` |
| `- id: gcp-compute-serial-ports` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-serial-ports.yaml` |
| `- id: gcp-compute-shielded-vm` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-compute-shielded-vm.yaml` |
| `- id: gcp-compute-ssl-policy` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-ssl-policy.yaml` |
| `- id: gcp-compute-template-ip-forward` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-template-ip-forward.yaml` |
| `- id: gcp-compute-template-public-ip` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-compute-template-public-ip.yaml` |
| `- id: gcp-compute-template-shielded-vm` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-compute-template-shielded-vm.yaml` |
| `- id: gcp-dataflow-job-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-dataflow-job-encrypted-with-cmk.yaml` |
| `- id: gcp-dataflow-private-job` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-dataflow-private-job.yaml` |
| `- id: gcp-datafusion-private-instance` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-datafusion-private-instance.yaml` |
| `- id: gcp-datafusion-stack-driver-logging` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-datafusion-stack-driver-logging.yaml` |
| `- id: gcp-datafusion-stack-driver-monitoring` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-datafusion-stack-driver-monitoring.yaml` |
| `- id: gcp-dataproc-cluster-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-dataproc-cluster-encrypted-with-cmk.yaml` |
| `- id: gcp-dataproc-cluster-public-ip` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-dataproc-cluster-public-ip.yaml` |
| `- id: gcp-dataproc-private-cluster-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-dataproc-private-cluster-iam-binding.yaml` |
| `- id: gcp-dataproc-private-cluster-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-dataproc-private-cluster-iam-member.yaml` |
| `- id: gcp-dns-key-specs-rsasha1` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-dns-key-specs-rsasha1.yaml` |
| `gcp-dnssec-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-dnssec-enabled.yaml` |
| `- id: gcp-folder-impersonation-roles-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-folder-impersonation-roles-iam-binding.yaml` |
| `- id: gcp-folder-impersonation-roles-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-folder-impersonation-roles-iam-member.yaml` |
| `- id: gcp-folder-member-default-service-account-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-folder-member-default-service-account-iam-binding.yaml` |
| `- id: gcp-folder-member-default-service-account-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-folder-member-default-service-account-iam-member.yaml` |
| `- id: gcp-gke-alias-ip-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-alias-ip-enabled.yaml` |
| `- id: gcp-gke-basic-auth` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-basic-auth.yaml` |
| `- id: gcp-gke-binary-authorization` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-binary-authorization.yaml` |
| `- id: gcp-gke-client-certificate-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-client-certificate-disabled.yaml` |
| `- id: gcp-gke-cluster-logging` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-cluster-logging.yaml` |
| `- id: gcp-gke-enable-shielded-nodes` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-enable-shielded-nodes.yaml` |
| `- id: gcp-gke-enabled-vpc-flow-logs` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-enabled-vpc-flow-logs.yaml` |
| `- id: gcp-gke-ensure-integrity-monitoring` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-ensure-integrity-monitoring.yaml` |
| `- id: gcp-gke-has-labels` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-has-labels.yaml` |
| `- id: gcp-gke-kubernetes-rbac-google-groups` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-kubernetes-rbac-google-groups.yaml` |
| `- id: gcp-gke-legacy-auth-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-legacy-auth-enabled.yaml` |
| `- id: gcp-gke-legacy-instance-metadata-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-legacy-instance-metadata-disabled.yaml` |
| `- id: gcp-gke-master-authz-networks-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-master-authz-networks-enabled.yaml` |
| `- id: gcp-gke-metadata-server-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-metadata-server-enabled.yaml` |
| `- id: gcp-gke-monitoring-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-monitoring-enabled.yaml` |
| `- id: gcp-gke-network-policy-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-network-policy-enabled.yaml` |
| `- id: gcp-gke-nodepool-auto-repair-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-nodepool-auto-repair-enabled.yaml` |
| `gcp-gke-nodepool-auto-upgrade-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-nodepool-auto-upgrade-enabled.yaml` |
| `- id: gcp-gke-nodepool-integrity-monitoring` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-nodepool-integrity-monitoring.yaml` |
| `gcp-gke-nodepool-metadata-server-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-nodepool-metadata-server-enabled.yaml` |
| `gcp-gke-nodepool-secure-boot-for-shielded-nodes` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-nodepool-secure-boot-for-shielded-nodes.yaml` |
| `- id: gcp-gke-pod-security-policy-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-pod-security-policy-enabled.yaml` |
| `- id: gcp-gke-private-cluster-config` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-private-cluster-config.yaml` |
| `- id: gcp-gke-public-control-plane` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-public-control-plane.yaml` |
| `- id: gcp-gke-secure-boot-for-shielded-nodes` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-gke-secure-boot-for-shielded-nodes.yaml` |
| `- id: gcp-gke-sql-backup-configuration-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-sql-backup-configuration-enabled.yaml` |
| `- id: gcp-gke-use-cos-image` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-gke-use-cos-image.yaml` |
| `- id: gcp-insecure-load-balancer-tls-version` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/gcp-insecure-load-balancer-tls-version.yaml` |
| `- id: gcp-ipv6-private-google-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-ipv6-private-google-enabled.yaml` |
| `- id: gcp-kms-prevent-destroy` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-kms-prevent-destroy.yaml` |
| `- id: gcp-memory-store-for-redis-auth-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-memory-store-for-redis-auth-enabled.yaml` |
| `- id: gcp-memory-store-for-redis-intransit-encryption` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-memory-store-for-redis-intransit-encryption.yaml` |
| `gcp-mysql-local-in-file-off` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-mysql-local-in-file-off.yaml` |
| `- id: gcp-org-impersonation-roles-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-org-impersonation-roles-iam-binding.yaml` |
| `- id: gcp-org-impersonation-roles-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-org-impersonation-roles-iam-member.yaml` |
| `- id: gcp-org-member-default-service-account-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-org-member-default-service-account-iam-binding.yaml` |
| `- id: gcp-org-member-default-service-account-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-org-member-default-service-account-iam-member.yaml` |
| `gcp-postgresql-log-checkpoints` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-postgresql-log-checkpoints.yaml` |
| `gcp-postgresql-log-connection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-postgresql-log-connection.yaml` |
| `gcp-postgresql-log-disconnection` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-postgresql-log-disconnection.yaml` |
| `gcp-postgresql-log-lock-waits` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-postgresql-log-lock-waits.yaml` |
| `gcp-postgresql-log-min-duration` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-postgresql-log-min-duration.yaml` |
| `gcp-postgresql-log-min-message` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-postgresql-log-min-message.yaml` |
| `gcp-postgresql-log-temp` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-postgresql-log-temp.yaml` |
| `- id: gcp-project-default-network` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-project-default-network.yaml` |
| `- id: gcp-project-member-default-service-account-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-project-member-default-service-account-iam-binding.yaml` |
| `- id: gcp-project-member-default-service-account-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-project-member-default-service-account-iam-member.yaml` |
| `- id: gcp-project-service-account-user-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-project-service-account-user-iam-binding.yaml` |
| `- id: gcp-project-service-account-user-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-project-service-account-user-iam-member.yaml` |
| `- id: gcp-pubsub-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-pubsub-encrypted-with-cmk.yaml` |
| `- id: gcp-pubsub-private-topic-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-pubsub-private-topic-iam-binding.yaml` |
| `- id: gcp-pubsub-private-topic-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-pubsub-private-topic-iam-member.yaml` |
| `- id: gcp-run-private-service-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-run-private-service-iam-binding.yaml` |
| `- id: gcp-run-private-service-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-run-private-service-iam-member.yaml` |
| `- id: gcp-spanner-database-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-spanner-database-encrypted-with-cmk.yaml` |
| `- id: gcp-sql-database-require-ssl` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-sql-database-require-ssl.yaml` |
| `- id: gcp-sql-database-ssl-insecure-value-postgres-mysql` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/gcp-sql-database-ssl-insecure-value-postgres-mysql.yaml` |
| `- id: gcp-sql-database-ssl-insecure-value-sqlserver` | `WARNING` | 보안 옵션 누사 및 예방 단속 필요. | `terraform/security/gcp-sql-database-ssl-insecure-value-sqlserver.yaml` |
| `- id: gcp-sql-public-database` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-sql-public-database.yaml` |
| `- id: gcp-sqlserver-no-public-ip` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-sqlserver-no-public-ip.yaml` |
| `- id: gcp-storage-bucket-not-public-iam-binding` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-storage-bucket-not-public-iam-binding.yaml` |
| `- id: gcp-storage-bucket-not-public-iam-member` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-storage-bucket-not-public-iam-member.yaml` |
| `- id: gcp-storage-bucket-uniform-access` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-storage-bucket-uniform-access.yaml` |
| `gcp-storage-versioning-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/best-practice/gcp-storage-versioning-enabled.yaml` |
| `- id: gcp-sub-network-logging-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-sub-network-logging-enabled.yaml` |
| `- id: gcp-sub-network-private-google-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-sub-network-private-google-enabled.yaml` |
| `- id: gcp-vertexai-dataset-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-vertexai-dataset-encrypted-with-cmk.yaml` |
| `- id: gcp-vertexai-metadata-store-encrypted-with-cmk` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-vertexai-metadata-store-encrypted-with-cmk.yaml` |
| `- id: gcp-vertexai-private-instance` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/gcp-vertexai-private-instance.yaml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `all-origins-allowed` | `WARNING` | all-origins-allowed IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/security/s3-cors-all-origins.yaml` |
| `ec2-imdsv1-optional` | `ERROR` | IaC 보안 옵션 누락 경보: ec2-imdsv1-optional 설정율을 적용해 주십시오. | `terraform/security/ec2-imdsv1-optional.yaml` |
| `- id: ecr-image-scan-on-push` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/ecr-image-scan-on-push.yaml` |
| `eks-insufficient-control-plane-logging` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/eks-insufficient-control-plane-logging.yaml` |
| `- id: eks-public-endpoint-enabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/eks-public-endpoint-enabled.yaml` |
| `- id: elastic-search-encryption-at-rest` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/elastic-search-encryption-at-rest.yaml` |
| `- id: no-iam-admin-privileges` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-admin-privileges.yaml` |
| `- id: no-iam-creds-exposure` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-creds-exposure.yaml` |
| `- id: no-iam-data-exfiltration` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-data-exfiltration.yaml` |
| `- id: no-iam-priv-esc-funcs` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-priv-esc-funcs.yaml` |
| `- id: no-iam-priv-esc-other-users` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-priv-esc-other-users.yaml` |
| `- id: no-iam-priv-esc-roles` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-priv-esc-roles.yaml` |
| `- id: no-iam-resource-exposure` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-resource-exposure.yaml` |
| `- id: no-iam-star-actions` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `terraform/security/iam/no-iam-star-actions.yaml` |
| `rds-insecure-password-storage-in-source-code` | `WARNING` | rds-insecure-password-storage-in-source-code IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/security/rds-insecure-password-storage-in-source-code.yaml` |
| `rds-public-access` | `WARNING` | rds-public-access IaC 보안 설정 정비 및 예방 조치 요망. | `terraform/security/rds-public-access.yaml` |
| `s3-public-read-bucket` | `WARNING` | S3 버킷 퍼블릭 오픈 및 암호화 누락 경보. | `terraform/security/s3-public-read-bucket.yaml` |
| `s3-public-rw-bucket` | `ERROR` | IaC 보안 옵션 누락 경보: s3-public-rw-bucket 설정율을 적용해 주십시오. | `terraform/security/s3-public-rw-bucket.yaml` |
| `s3-unencrypted-bucket` | `INFO` | S3 버킷 퍼블릭 오픈 및 암호화 누락 경보. | `terraform/security/s3-unencrypted-bucket.yaml` |
# Language: TYPESCRIPT

## ANGULAR

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: angular-bypasssecuritytrust` | `WARNING` | Angular 프론트엔드 결함 주사 점검. | `typescript/security/audit/angular-domsanitizer.yaml` |
## AWS-CDK

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `aws-cdk-bucket-enforcessl` | `ERROR` | S3 버킷에 전송 중 암호화(SSL/TLS) 강제화 옵션이 누락되었습니다. 데이터 전송 기밀성을 위해 해당 옵션을 활성화하십시오. | `typescript/security/audit/awscdk-bucket-enforcessl.yml` |
| `awscdk-bucket-encryption` | `ERROR` | AWS CDK S3 버킷 생성 시 기본 암호화(KMS_MANAGED 등) 설정이 누락되었습니다. 데이터 보안을 위해 암호화 모드를 지정하세요. | `typescript/security/audit/awscdk-bucket-encryption.yml` |
| `- id: awscdk-bucket-grantpublicaccessmethod` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/security/awscdk-bucket-grantpublicaccessmethod.yml` |
| `- id: awscdk-codebuild-project-public` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/security/awscdk-codebuild-project-public.yml` |
| `- id: awscdk-sqs-unencryptedqueue` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/security/audit/awscdk-sqs-unencryptedqueue.yml` |
## LANG

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: cors-regex-wildcard` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/security/audit/cors-regex-wildcard.yaml` |
| `- id: moment-deprecated` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/best-practice/moment-deprecated.yaml` |
| `useless-ternary` | `ERROR` | 불필요한 삼항 연산자(a ? true : false 등) 사용이 감지되었습니다. 논리 직관성을 위해 단순 불리언 캐스팅 등으로 대체하세요. | `typescript/correctness/useless-ternary.yaml` |
## NESTJS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: nestjs-header-cors-any` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/security/audit/nestjs-header-cors-any.yaml` |
| `- id: nestjs-header-xss-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/security/audit/nestjs-header-xss-disabled.yaml` |
| `- id: nestjs-open-redirect` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/security/audit/nestjs-open-redirect.yaml` |
## REACT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `define-styled-components-on-module-level` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/best-practice/define-styled-components-on-module-level.yaml` |
| `- id: i18next-key-format` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/portability/i18next/i18next-key-format.yaml` |
| `- id: jsx-label-not-i18n` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/portability/i18next/jsx-label-not-i18n.yaml` |
| `- id: jsx-not-internationalized` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/portability/i18next/jsx-not-internationalized.yaml` |
| `- id: mui-snackbar-message` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/portability/i18next/mui-snackbar-message.yaml` |
| `- id: react-dangerouslysetinnerhtml` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/security/audit/react-dangerouslysetinnerhtml.yaml` |
| `react-find-dom` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/best-practice/react-find-dom.yaml` |
| `react-href-var` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/security/audit/react-href-var.yaml` |
| `react-insecure-request` | `ERROR` | React 애플리케이션에서 암호화되지 않은 HTTP 요청이 감지되었습니다. 스니핑 공격에 노출되므로 https:// 사용을 권장합니다. | `typescript/security/react-insecure-request.yaml` |
| `- id: react-jwt-decoded-property` | `INFO` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `typescript/security/audit/react-jwt-decoded-property.yaml` |
| `- id: react-jwt-in-localstorage` | `INFO` | JWT 토큰 서명 및 디코드 검증 누락 여부 점검. | `typescript/security/audit/react-jwt-in-localstorage.yaml` |
| `react-legacy-component` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/best-practice/react-legacy-component.yaml` |
| `- id: react-markdown-insecure-html` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/security/react-markdown-insecure-html.yaml` |
| `react-props-in-state` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/best-practice/react-props-in-state.yaml` |
| `- id: react-props-spreading` | `INFO` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/best-practice/react-props-spreading.yaml` |
| `- id: react-unsanitized-method` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/security/audit/react-unsanitized-method.yaml` |
| `- id: react-unsanitized-property` | `WARNING` | React 컴포넌트 데이터 바인딩 가압 점검. | `typescript/security/audit/react-unsanitized-property.yaml` |
| `- id: useselect-label-not-i18n` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `typescript/portability/i18next/useselect-label-not-i18n.yaml` |
# Language: YAML

## ARGO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `argo-workflow-parameter-command-injection` | `ERROR` | Argo 워크플로우 매개변수 가동 중 원격 명령어 주입(Command Injection) 우려가 수렴됩니다. | `yaml/security/argo-workflow-parameter-command-injection.yaml` |
| `event-binding-payload-with-hyphen` | `WARNING` | 설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검. | `yaml/correctness/event-binding-payload-with-hyphen.yaml` |
## DOCKER-COMPOSE

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: exposing-docker-socket-volume` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/exposing-docker-socket-volume.yaml` |
| `- id: no-new-privileges` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/no-new-privileges.yaml` |
| `- id: privileged-service` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/privileged-service.yaml` |
| `- id: seccomp-confinement-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/seccomp-confinement-disabled.yaml` |
| `- id: selinux-separation-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/selinux-separation-disabled.yaml` |
| `- id: writable-filesystem-service` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/writable-filesystem-service.yaml` |
## GITHUB-ACTIONS

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `allowed-unsecure-commands` | `WARNING` | 설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검. | `yaml/security/allowed-unsecure-commands.yaml` |
| `curl-eval` | `ERROR` | curl-eval 보안 인젝션 및 런타임 버그 전사 예방 점검. | `yaml/security/curl-eval.yaml` |
| `detect-shai-hulud-backdoor` | `ERROR` | detect-shai-hulud-backdoor 보안 인젝션 및 런타임 버그 전사 예방 점검. | `yaml/security/detect-shai-hulud-backdoor.yaml` |
| `github-script-injection` | `ERROR` | github-script-injection 보안 인젝션 및 런타임 버그 전사 예방 점검. | `yaml/security/github-script-injection.yaml` |
| `- id: pull-request-target-code-checkout` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/pull-request-target-code-checkout.yaml` |
| `run-shell-injection` | `ERROR` | run-shell-injection 보안 인젝션 및 런타임 버그 전사 예방 점검. | `yaml/security/run-shell-injection.yaml` |
| `semgrep-github-action-push-without-branches` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/semgrep-configuration/semgrep-github-action-push-without-branches.yml` |
| `third-party-action-not-pinned-to-commit-sha` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/third-party-action-not-pinned-to-commit-sha.yml` |
| `unsafe-add-mask-workflow-command` | `WARNING` | 설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검. | `yaml/security/audit/unsafe-add-mask-workflow-command.yaml` |
| `- id: workflow-run-target-code-checkout` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/workflow-run-target-code-checkout.yaml` |
## GITLAB

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `changes-with-when-never` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/correctness/changes-with-when-never.yaml` |
## KUBERNETES

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `- id: allow-privilege-escalation` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/allow-privilege-escalation.yaml` |
| `- id: allow-privilege-escalation-no-securitycontext` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/allow-privilege-escalation-no-securitycontext.yaml` |
| `- id: allow-privilege-escalation-true` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/allow-privilege-escalation-true.yaml` |
| `- id: exposing-docker-socket-hostpath` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/exposing-docker-socket-hostpath.yaml` |
| `flask-debugging-enabled` | `WARNING` | 설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검. | `yaml/security/env/flask-debugging-enabled.yaml` |
| `- id: hostipc-pod` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/hostipc-pod.yaml` |
| `- id: hostnetwork-pod` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/hostnetwork-pod.yaml` |
| `- id: hostpid-pod` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/hostpid-pod.yaml` |
| `legacy-api-clusterrole-excessive-permissions` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/legacy-api-clusterrole-excessive-permissions.yaml` |
| `no-fractional-cpu-limits` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/best-practice/no-fractional-cpu-limits.yaml` |
| `- id: privileged-container` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/privileged-container.yaml` |
| `- id: run-as-non-root` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/run-as-non-root.yaml` |
| `- id: run-as-non-root-container-level` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/run-as-non-root-container-level.yaml` |
| `- id: run-as-non-root-container-level-missing-security-context` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/run-as-non-root-container-level-missing-security-context.yaml` |
| `- id: run-as-non-root-security-context-pod-level` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/run-as-non-root-security-context-pod-level.yaml` |
| `- id: run-as-non-root-unsafe-value` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/run-as-non-root-unsafe-value.yaml` |
| `- id: seccomp-confinement-disabled` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/seccomp-confinement-disabled.yaml` |
| `- id: secrets-in-config-file` | `WARNING` | 자격증명/보안 기밀 문자열 누락 여부 점검. | `yaml/security/secrets-in-config-file.yaml` |
| `- id: skip-tls-verify-cluster` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/skip-tls-verify-cluster.yaml` |
| `- id: skip-tls-verify-service` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/skip-tls-verify-service.yaml` |
| `- id: writable-filesystem-container` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/writable-filesystem-container.yaml` |
## OPENAPI

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `api-key-in-query-parameter` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/api-key-in-query-parameter.yaml` |
| `openai-consequential-action-false` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/security/openai-consequential-action-false.yaml` |
| `use-of-basic-authentication` | `ERROR` | use-of-basic-authentication 보안 인젝션 및 런타임 버그 전사 예방 점검. | `yaml/security/use-of-basic-authentication.yaml` |
## SEMGREP

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `interfile-true-under-metadata-and-no-options` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/interfile-true-under-metadata-and-no-options.yaml` |
| `interfile-true-under-metadata-and-options-already-present` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/interfile-true-under-metadata-and-options-already-present.yaml` |
| `- id: metadata-category` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-category.yaml` |
| `missing-message-field` | `WARNING` | 이 규칙에는 메시지가 없습니다. Semgrep에는 규칙에 메시지가 있어야 합니다. 규칙의 기능을 설명하는 메시지를 포함합니다. 이것이 문제인 이유와 해결 방법을 설명하는 메시지를 작성해 보세요. | `yaml/missing-message-field.yaml` |
| `- id: missing-language-field` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/missing-language-field.yaml` |
| `- id: duplicate-id` | `ERROR` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/duplicate-id.yaml` |
| `duplicate-pattern` | `ERROR` | Semgrep 규칙 저작 메타 데이터 정합성 점검: duplicate-pattern 누락 경보. | `yaml/duplicate-pattern.yaml` |
| `empty-message` | `WARNING` | 이 규칙에는 빈 메시지 필드가 있습니다. 이 규칙이 문제가 되는 이유와 해결 방법을 전달하는 메시지 필드를 추가해 보세요. 이렇게 하면 결과가 해결될 가능성이 높아집니다. | `yaml/empty-message.yaml` |
| `lang-consistency-bash` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'bash'를 사용하세요. | `yaml/consistency/lang-consistency-bash.yaml` |
| `lang-consistency-cpp` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'cpp'를 사용하세요. | `yaml/consistency/lang-consistency-cpp.yaml` |
| `lang-consistency-csharp` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'csharp'를 사용하세요. | `yaml/consistency/lang-consistency-csharp.yaml` |
| `lang-consistency-dockerfile` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'dockerfile'을 사용하세요. | `yaml/consistency/lang-consistency-dockerfile.yaml` |
| `lang-consistency-elixir` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 '엘릭서'를 사용하세요. | `yaml/consistency/lang-consistency-elixir.yaml` |
| `lang-consistency-go` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'go'를 사용하세요. | `yaml/consistency/lang-consistency-go.yaml` |
| `lang-consistency-hcl` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. Please use 'hcl' instead. | `yaml/consistency/lang-consistency-hcl.yaml` |
| `lang-consistency-js` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'js'를 사용하세요. | `yaml/consistency/lang-consistency-js.yaml` |
| `lang-consistency-kotlin` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'kotlin'을 사용하세요. | `yaml/consistency/lang-consistency-kotlin.yaml` |
| `lang-consistency-python` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 '파이썬'을 사용하세요. | `yaml/consistency/lang-consistency-python.yaml` |
| `lang-consistency-regex` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 '정규식'을 사용하세요. | `yaml/consistency/lang-consistency-regex.yaml` |
| `lang-consistency-solidity` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'solidity'를 사용하세요. | `yaml/consistency/lang-consistency-solidity.yaml` |
| `lang-consistency-ts` | `WARNING` | semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'ts'를 사용하세요. | `yaml/consistency/lang-consistency-ts.yaml` |
| `message-whitespace-check` | `WARNING` | 규칙 메시지에 추가 공백이 있는 것 같습니다. 이는 결과 결과에서 어색해 보일 수 있습니다. 추가 공백을 제거하십시오! | `yaml/message-whitespace.yaml` |
| `- id: metadata-confidence` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-confidence.yaml` |
| `- id: metadata-confidence-incorrect-value` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-confidence-incorrect-value.yaml` |
| `metadata-cwe` | `ERROR` | Semgrep 규칙 저작 메타 데이터 정합성 점검: metadata-cwe 누락 경보. | `yaml/metadata-cwe.yaml` |
| `metadata-cwe-prohibited-or-discouraged` | `ERROR` | Semgrep 규칙 저작 메타 데이터 정합성 점검: metadata-cwe-prohibited-or-discouraged 누락 경보. | `yaml/metadata-cwe-prohibited-or-discouraged.yaml` |
| `- id: metadata-deepsemgrep` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-deepsemgrep.yaml` |
| `- id: metadata-impact` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-impact.yaml` |
| `- id: metadata-impact-incorrect-value` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-impact-incorrect-value.yaml` |
| `- id: metadata-incorrect-option` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-incorrect-option.yaml` |
| `metadata-license` | `ERROR` | Semgrep 규칙 저작 메타 데이터 정합성 점검: metadata-license 누락 경보. | `yaml/metadata-license.yaml` |
| `- id: metadata-likelihood` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-likelihood.yaml` |
| `- id: metadata-likelihood-incorrect-value` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-likelihood-incorrect-value.yaml` |
| `metadata-owasp` | `ERROR` | Semgrep 규칙 저작 메타 데이터 정합성 점검: metadata-owasp 누락 경보. | `yaml/metadata-owasp.yaml` |
| `metadata-references` | `ERROR` | Semgrep 규칙 저작 메타 데이터 정합성 점검: metadata-references 누락 경보. | `yaml/metadata-references.yaml` |
| `- id: metadata-subcategory` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-subcategory.yaml` |
| `- id: metadata-subcategory-incorrect-value` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-subcategory-incorrect-value.yaml` |
| `- id: metadata-technology` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/metadata-technology.yaml` |
| `- id: missing-deconstructed-value` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/rule-missing-deconstructed-value.yaml` |
| `multi-line-message` | `WARNING` | 이 규칙에는 터미널에 제대로 표시되지 않을 수 있는 여러 줄의 메시지 필드가 있습니다. 한 줄에 있는지 확인하십시오. 예를 들어 `메시지: |`yaml/가 아닌 `메시지: >-`를 사용하세요. | `multi-line-message.yaml` |
| `- id: slow-pattern-general-func` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/slow-pattern-general-function.yaml` |
| `- id: slow-pattern-general-property` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/slow-pattern-general-property.yaml` |
| `- id: slow-pattern-single-metavariable` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/slow-pattern-single-metavariable.yaml` |
| `- id: slow-pattern-top-ellipsis` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/slow-pattern-top-ellipsis.yaml` |
| `- id: unnecessary-parent-operator` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/unnecessary-parent.yaml` |
| `unsatisfiable-rule` | `ERROR` | Semgrep 규칙 저작 메타 데이터 정합성 점검: unsatisfiable-rule 누락 경보. | `yaml/unsatisfiable.yaml` |
| `yaml-key-indentation-check` | `WARNING` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `yaml/key-indentation.yaml` |
