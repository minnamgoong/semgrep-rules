rules:
- id: crypto-mode-without-authentication
  languages:
  - python
  message: 무결성 검증 없는 암호화 운영 모드가 사용되었습니다. GCM 등 AEAD 모드 사용을 권장합니다.
  metadata:
    category: security
    confidence: MEDIUM
    cwe:
    - 'CWE-327: Use of a Broken or Risky Cryptographic Algorithm'
    impact: MEDIUM
    likelihood: LOW
    owasp:
    - A03:2017 - Sensitive Data Exposure
    - A02:2021 - Cryptographic Failures
    - A04:2025 - Cryptographic Failures
    references:
    - https://owasp.org/Top10/A02_2021-Cryptographic_Failures
    subcategory:
    - vuln
    technology:
    - cryptography
  patterns:
  - pattern-either:
    - patterns:
      - pattern-either:
        - pattern: 'AES.new(..., $PYCRYPTODOME_MODE)

            '
      - pattern-not-inside: 'AES.new(..., $PYCRYPTODOME_MODE)

          ...

          HMAC.new

          '
      - metavariable-pattern:
          metavariable: $PYCRYPTODOME_MODE
          patterns:
          - pattern-either:
            - pattern: AES.MODE_CBC
            - pattern: AES.MODE_CTR
            - pattern: AES.MODE_CFB
            - pattern: AES.MODE_OFB
  severity: ERROR
