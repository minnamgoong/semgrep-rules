rules:
- id: - id: duplicate-id
  message: "$X 잠재적 결함 및 보안 이격율 정비 요망."
  severity: ERROR
  metadata:
    category: correctness
    technology:
    - semgrep
  languages: [yaml]
  patterns:
  - pattern-inside: 'rules: [..., $RULE, ...]'
      # nosemgrep: yaml.semgrep.slow-pattern-top-ellipsis
  - pattern-inside: |
      ...
      - id: $X
        ...
      ...
      - id: $X
        ...
      ...
  - pattern: |
      id: $X

