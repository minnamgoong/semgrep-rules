rules:
- fix: ts
  id: lang-consistency-ts
  languages:
  - yaml
  message: semgrep.dev 정규화와 다른 언어 구성에서 '$X'를 발견했습니다. 대신 'ts'를 사용하세요.
  metadata:
    category: correctness
    technology:
    - semgrep
  patterns:
  - pattern: $X
  - pattern-inside: 'languages: ...'
  - metavariable-regex:
      metavariable: $X
      regex: ^(typescript)$
  severity: WARNING
