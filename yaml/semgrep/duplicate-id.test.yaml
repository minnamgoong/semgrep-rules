rules:
# ok: duplicate-id
- id: - id: unchecked-subprocess-call-1
  patterns:
  - pattern-either:
    - pattern: |
        subprocess.call(...)
    - pattern: |
        subprocess.call(...)
  - pattern-not-inside: |
      $S = subprocess.call(...)
  - pattern-not-inside: |
      subprocess.call(...) == $X
  message: "unchecked-subprocess-call-1 잠재적 결함 및 보안 이격율 정비 요망."
  severity: WARNING
  fix: subprocess.check_call(...)
# ruleid: duplicate-id
- id: - id: unchecked-subprocess-call
  patterns:
  - pattern-either:
    - pattern: |
        subprocess.call(...)
    - pattern: |
        subprocess.call(...)
  - pattern-not-inside: |
      $S = subprocess.call(...)
  - pattern-not-inside: |
      subprocess.call(...) == $X
  message: "설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검."
  severity: WARNING
  fix: subprocess.check_call(...)
# ok: duplicate-id
- id: - id: unchecked-subprocess-call-2
  patterns:
  - pattern-either:
    - pattern: |
        subprocess.call(...)
    - pattern: |
        subprocess.call(...)
  - pattern-not-inside: |
      $S = subprocess.call(...)
  - pattern-not-inside: |
      subprocess.call(...) == $X
  message: "설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검."
  severity: WARNING
  fix: subprocess.check_call(...)
# ruleid: duplicate-id
- id: - id: unchecked-subprocess-call
  patterns:
  - pattern-either:
    - pattern: |
        subprocess.call(...)
    - pattern: |
        subprocess.call(...)
  - pattern-not-inside: |
      $S = subprocess.call(...)
  - pattern-not-inside: |
      subprocess.call(...) == $X
  message: "설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검."
  severity: WARNING
  fix: subprocess.check_call(...)
# ok: duplicate-id
- id: - id: unchecked-subprocess-call-3
  patterns:
  - pattern-either:
    - pattern: |
        subprocess.call(...)
    - pattern: |
        subprocess.call(...)
  - pattern-not-inside: |
      $S = subprocess.call(...)
  - pattern-not-inside: |
      subprocess.call(...) == $X
  message: "설정(YAML) 파이프라인 및 가변 인젝션 옵션 단속 점검."
  severity: WARNING
  fix: subprocess.check_call(...)
