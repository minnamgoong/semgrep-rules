# dangerous-spawn-shell (비정형 Spawn 실행 경보)

## 개요
`spawn` 메서드 가동 인자 리스트 중 첫 번째 명령어 파일 명칭 자체가 정적 상수가 아닌 변수 가변으로 지정된 비정형 형태를 적시합니다.

## 위험성
*   공격자가 바이너리 이름 자체를 `rm`이나 `bash`로 변환 주입하여 쉘 탈취 명령을 트리거할 수 있습니다.

## 조치 방안
*   명령어 파일 명칭은 하드코딩된 정적 문자열로 박아 넣으세요.
