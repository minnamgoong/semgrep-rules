# require-request (동적 require 로딩 위험)

## 개요
Node.js의 코어 탑재 함수인 `require("변수")` 내에 유저 입력 결합 가변이 담기는 국면을 방어합니다.

## 위험성
*   **임의 모듈 로드**: 공격자가 내부 `node_modules` 중 보안상 취약한 스크립트를 수동 지목 로드하거나, 업로드 경로를 제어해 본인의 불법 스크립트 실행을 기동할 배후가 성사됩니다.

## 조치 방안
*   `require`문 안에는 고정 리터럴 문자열만 허용하십시오.
