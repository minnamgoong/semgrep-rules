# aead-no-final (Auth Tag 검증 누락 점검)

## 개요
GCM 등 인증 기반 암호화 해독 후 `decipher.final()` 버퍼 집계를 생략하는 경우를 검출합니다.

## 위험성
*   해당 메서드가 호출될 때 내부적으로 무결성 Tag 검증 예외를 탄생시키는데, 이를 안 하면 위조된 위조 암호문을 정상 연산 수긍하게 됩니다.

## 조치 방안
*   해독 루프 끝단에 반드시 `final()`을 추가 연산해야 합니다.
