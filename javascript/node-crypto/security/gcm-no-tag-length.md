# gcm-no-tag-length (GCM 인증 태그 누락 경고)

## 개요
GCM 모드 구동 시 createDecipheriv 에 인증 태그(Tag) 탑재 확인 옵션이 누락되었습니다. 데이터 무결성 검증을 위해 반드시 활성화하세요.

## 위험성
*   무결성 오버헤드 검출이 부실해지며 암호문 위조 식별 버퍼가 오동작 합니다.

## 조치 방안
*   `setAuthTag` 또는 옵션 선언을 명시적으로 둘러싸 복호화 트리를 교정하십시오.
