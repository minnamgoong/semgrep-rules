# vm-runincontext-injection (VM 컨텍스트 인젝션 경보)

## 개요
vm.runInContext() 호출에 외부 입력 데이터가 유입되어 샌드박스 우회 위험이 있습니다. 가변 코드 실행 연산을 중단하세요.

## 위험성
*   **원격 코드 실행 (RCE)**: 샌드박스 내부 구동이라 하더라도 부모 프레임 객체 참조 연쇄를 통해 Lambda 본체 컨테이너를 장악하는 사고가 가능합니다.

## 조치 방안
*   가변값을 스크립트 본문 유닛으로 집어넣지 마시고 호출 매개 변수로 빼서 바인딩 하십시오.
