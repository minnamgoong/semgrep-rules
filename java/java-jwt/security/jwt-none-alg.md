# java-jwt-none-alg (JWT 'none' 알고리즘 사용 감지)

## 개요
Auth0의 `java-jwt` 라이브러리를 통해 JWT 토큰 가공 시 `none` 알고리즘 서명 방식을 사용하는 국면을 감지합니다.

## 위험성
*   **인증 무력화**: 암호화 서명 보안 장치가 전혀 가동되지 않으므로, 유저가 페이로드를 수정 후 백엔드에 전송해도 인증이 통과되는 대형 사고가 납니다.

## 조치 방안
*   토큰 검증 빌더 호출 시 서명을 강제하는 보안 알고리즘 고정 레이어를 신설하세요.
