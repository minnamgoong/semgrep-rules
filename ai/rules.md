# Ai Semgrep Rules Summary

`ai` 디렉토리 하위의 규칙 목록입니다. (테스트 파일 제외)

## CSHARP

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-openai.yaml` |

## DART

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-gemini.yaml` |

## GENERIC

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-generic-ai-anthprop` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-generic-ai-anthprop.yaml` |
| `detect-generic-ai-api` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-generic-ai-api.yaml` |
| `detect-generic-ai-gem` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-generic-ai-gem.yaml` |
| `detect-generic-ai-oai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-generic-ai-oai.yaml` |

## GO

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-gemini.yaml` |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-openai.yaml` |

## KOTLIN

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-gemini.yaml` |

## PYTHON

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-anthropic` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-anthropic.yaml` |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-gemini.yaml` |
| `detect-huggingface` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-huggingface.yaml` |
| `detect-langchain` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-langchain.yaml` |
| `detect-mistral` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-mistral.yaml` |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-openai.yaml` |
| `detect-pytorch` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-pytorch.yaml` |
| `detect-tensorflow` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-tensorflow.yaml` |

## SWIFT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-apple-core-ml` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-apple-core-ml.yaml` |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-gemini.yaml` |

## TYPESCRIPT

| Rule ID | Severity | Summary | Path |
| :--- | :---: | :--- | :--- |
| `detect-anthropic` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-anthropic.yaml` |
| `detect-gemini` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-gemini.yaml` |
| `detect-mistral` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-mistral.yaml` |
| `detect-openai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-openai.yaml` |
| `detect-promptfoo` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-promptfoo.yaml` |
| `detect-vercel-ai` | `INFO` | 안전하지 않은 코드 패턴이 발견되었습니다. 보안 취약점을 방지하기 위해 코드를 점검하고 수정해 주십시오. | `detect-vercel-ai.yaml` |
